# maintain — rules installed into skills

Machine input for the installer, read by nobody at session time. Amend a rule here, then
install it with overwrite; the block in a target is never the place.

| target | anchor |
|---|---|
| `.agents/skills/mechanism/SKILL.md` | `## Incept` |

## M1 — what an inceptor hands /maintain

- **target** `.agents/skills/mechanism/SKILL.md`
- **authority** the user, 2026-09-07

<rule>
Name what `/maintain` must do for this mechanism — a maintainer script to run, records to
re-check when its surfaces move — as rules in its rules file targeting `/maintain`. Nothing is
written into `/maintain` by hand.
</rule>
