# A second mechanism is declared, and the shape either holds or is amended

- **Status:** Planned (the injector precedes)
- **Type:** HITL
- **Depends on:** [01-0011.0010](./01-0011.0010-mechanism-declared.md) (the shape, and the check
  that a declaration is true), [01-0011.0020](./01-0011.0020-rules-one-home.md) (the injector,
  whose first rule targets this skill)
- **Blocks:** [01-0011.0025](./01-0011.0025-archive-duty-reaches-maintain.md),
  [01-0011.0050](./01-0011.0050-shape-checked.md) — everything after this leans on the shape, and
  this is the cheapest test of whether it survives a second application
- **Outcome:** `/maintain` is declared through the shape with its parts, its moments and its record
  obligation each carrying a state, the declaration passes the check `01-0011.0010` shipped, and
  what the second application breaks is amended in the shape rather than worked around in the
  declaration.

## Parent

[A mechanism has one home for its rules, and a derived index](../spec/01-0011-mechanism-shape.md).
`/impact`: [the 2026-09-07 re-slice assessment](../spec/01-0011-mechanism-shape.md#impact--2026-09-07).

## Why this exists, and why it is not "proves the shape generalises"

`01-0011.0010` declares `/mechanism` by its own shape. A shape that has only ever described itself
has not been tested: every choice in it was made while looking at the one thing it describes. The
second application is where a shape either survives or shows which of its rules were about the
first case all along.

This slice is placed here for **falsification, not for confidence**. Three slices —
`01-0011.0025`, `.0030` and `.0040` — build on the shape. Buying the test before they do is worth a
slice; buying it after them means amending a shape three things already depend on.

**It does not exercise injection or the record rule, and must not claim to.** `/maintain` has no
records: `temporary_statements.py` reads `<temporary>` blocks in place, and there is no marks file,
no register, nothing that accumulates. So `/maintain`'s record obligation is `none` **by property**,
not deferred — and the rule `01-0011.0020` installs into it has no subject here. Its first subject
is `/ticket`, one slice later.

## What to build

- `.agents/mechanisms/maintain/` with its doc, declaring `/maintain`. Parts named with their
  standing: `/maintain`'s skill, `temporary_statements.py` and `test_temporary_statements.py` owned;
  `docs_corpus.py` and `tests/harness.py` **relied-on**, naming the owner — which is `/ticket`'s
  mechanism from `01-0011.0025`, and until then a declared gap.
- A moments table covering every occasion at which a person acts on `/maintain`, each row carrying
  its instruction or which kind of absence it is. A `not yet` names a ticket that exists.
- The record obligation stated as `none`, with the reason. An absence that is a property reads as
  an oversight forever unless it says so.
- Its evidence at `docs/mechanisms/maintain.evidence.md`, moved from
  `docs/research/maintenance-findings.md` and **sorted on the way**: what explains why the
  instruction is what it is belongs in the doc; what explains why the doc is what it is stays
  evidence. The file today is an unsorted mixture.
- [maintain/SKILL.md:138](../../.agents/skills/maintain/SKILL.md)'s link into `docs/` moved inside a
  `<project-local>` block, per `AGENTS.md`'s *Core and instance* rule. The same treatment for its
  links to `docs/process.md` and `docs/architecture.md`, which are references to documents only this
  project has.
- ~~A register row in `.agents/README.md`, in the `<project-local>` block `01-0011.0010`
  establishes.~~ **Superseded 2026-09-07 by that slice:** the register is derived, no file holds
  it, and `.agents/README.md` has no `<project-local>` block. `.agents/mechanisms/maintain/`
  existing *is* the row, and `mechanisms.py --index` renders it.

- **`/maintain`'s own policy consolidates into `/maintain`** *(the user, 2026-09-07)*.
  [Tree maintenance](../process.md#tree-maintenance) and
  [Mechanical maintenance](../process.md#mechanical-maintenance) are rules for one mechanism,
  sitting in the half a recipient replaces. Only `/maintain` cites the first; `/impact` cites the
  second as a pointer, which is a citation and not a second home. Split by the shape's own cut:
  what is read *in the work it governs* goes to the skill, why it is that way goes to the doc.
  Until it moves, a recipient receives `/maintain`'s body **without** the responsibilities its
  policy assigns it.

- **The nine responsibilities, including the ones `/denoise` carried.** `process.md:75` assigns
  `/maintain` architecture/code consistency in both directions, reconstruction of load-bearing
  contract shape, document consistency and canonical ownership, prose and inline-comment cleanup,
  concern disposition, records, indexes, links and archive lifecycle. **The body instructs roughly
  three of them.** Each survives into the skill with an instruction, or becomes a moment with a
  declared absence. Measured 2026-09-07, when a `/maintain` pass followed the body faithfully and
  still missed a stale roster: three of its steps have scripts that print something and one is
  judgment that prints nothing, so the unscripted step read as done.

- **Mechanism, documentation and code are three subjects, not one** *(the user, 2026-09-07)*. A
  pass over a mechanism validates that core and mechanism rules still apply and that evidence is
  extracted to its own home; a pass over documentation checks canonical ownership; a pass over code
  is neither. The distinction is **core** and belongs in the skill — the body says "code" zero
  times today. Which of the three a given project actually has is **not** core; see the decision
  below.

- **Concern disposition is a moment with no instruction.** `process.md:75` assigns it; the body
  never mentions concerns; and `docs/concerns.md`, which six installed skills transact against, has
  never existed. The moments table carries it as `not yet` naming
  [01-0017](./01-0017-io-graph-coherent.md), which already owns that file.
- Whatever the second application breaks, amended **in `/mechanism`'s body**, with what broke it
  recorded in `docs/mechanisms/mechanism-shape.evidence.md`. A shape bent to fit a declaration is
  the failure this slice exists to catch.

## Decisions this ticket's align owns

- **`/maintain`'s moments, and which absences are honest.** The taxonomy is only worth having if
  the second application is as honest as the first; a moment nobody acts on is not a moment and
  the row does not exist.
- **Whether `/maintain` is always-on.** Nothing can uninstall the thing that maintains, which is
  the argument that made `/mechanism` always-on — but `/maintain` is not the definition of
  installing, so the argument may not carry. It decides whether the doc owes lifecycle pieces.
- **The authority on each of its rules** — who may change it, at what strength.

- **Where "this project's code is entirely mechanism parts" is written down** *(raised by the user,
  2026-09-07)*. In this repository there is no product code: every file under `.agents/scripts/`
  and `tests/` is a part of some mechanism, so code-maintenance and mechanism-maintenance coincide
  here and nowhere else. **Recommendation: it is a facet, not a `<project-local>` block.** A block
  in every skill that touches code is N copies of one fact, which is the defect this spec exists to
  stop; [01-0010.0110](./01-0010.0110-project-facets-injected.md) already owns *a project answers
  the harness's questions about its own facets once, and those answers are installed into the
  places that need them*. This align decides whether that holds, and if so the facet is recorded
  there rather than built here.

## Acceptance criteria

- [ ] `.agents/mechanisms/maintain/` holds a doc naming every part with its standing, and every
  relied-on part names an owner or a declared gap with a ticket.
- [ ] The moments table covers every moment, each row carrying an instruction or a kind of absence;
  every `not yet` names a ticket that exists.
- [ ] The record obligation reads `none` with its reason, distinguishable from unstated.
- [ ] `docs/mechanisms/maintain.evidence.md` exists and `docs/research/maintenance-findings.md` no
  longer holds the mechanism's story; doc material has moved into the doc rather than travelling
  with the evidence.
- [ ] No link from `.agents/skills/maintain/` into `docs/` sits outside a `<project-local>` block.
- [ ] `docs/process.md`'s *Tree maintenance* and *Mechanical maintenance* are gone, their rules in
  `/maintain`'s skill and their reasons in its doc; the `<temporary>` block bound to this ticket is
  retired with them, and `temporary_statements.py` reports one fewer.
- [ ] `/maintain`'s body instructs, or declares an absence for, each of the nine responsibilities
  `process.md:75` assigned it — concern disposition included, as `not yet` naming
  [01-0017](./01-0017-io-graph-coherent.md).
- [ ] The body distinguishes maintaining a mechanism from maintaining documentation from
  maintaining code, and says what each obliges.
- [ ] `mechanisms.py --index` renders a `maintain` row from `.agents/mechanisms/maintain/`. No file
  holds a register.
- [ ] The check from `01-0011.0010` runs against two mechanisms and reports both declarations true.
- [ ] Every amendment the second application forced is in `/mechanism`'s body, with its cause in
  `mechanism-shape.evidence.md` — or the ticket records that the shape needed none, which is a
  result rather than an omission.
- [ ] `/verify` has been run on this ticket against its ticket, RFC, and governing docs.

## Out of scope

Declaring `/ticket` ([0025](./01-0011.0025-archive-duty-reaches-maintain.md)), which is the third
mechanism and the first with records. Building a maintainer script — `/maintain` has no records to
maintain.

**`docs/process.md`'s *Autonomy and repair* stays put, and the reason is now evidenced**
*(the user, 2026-09-07)*: it is **shared**, not `/maintain`'s. `/maintain` and `/verify` both cite
`#autonomy-and-repair`, `/implement` defers to the `repair` switch, and the switches live in
`AGENTS.md`. [01-0016](./01-0016-responsibility-coherent.md) owns where it lands. *Verification*
stays too — it is a project facet, [01-0010.0110](./01-0010.0110-project-facets-injected.md)'s.

**Corrected 2026-09-07:** *Mechanisms and skills* was listed here as out of scope on the ground
that it belonged to a mechanism nobody had declared. That ground expires exactly when this ticket
runs, and `01-0011.0010` bound the section's surviving sentence with
`<temporary until="01-0011.0022 is done">`. A statement cannot name an owner who has written down
that it is not theirs. It is in scope, above. Declaring any mechanism beyond the three the spec scopes — [01-0017](./01-0017-io-graph-coherent.md)
applies the shape to the rest.

## Parent scope addressed

Stories 14, 19, 20, 24.
