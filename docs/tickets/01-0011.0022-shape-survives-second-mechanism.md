# A second mechanism is declared, and the shape either holds or is amended

- **Status:** In progress (implemented 2026-09-07; awaits `/verify`)
- **Type:** HITL
- **Plan:** [implementation plan](../rfc/01-0011.0022-shape-survives-second-mechanism.md) — the doc
  to the format, the body as rules A1–E3, two shape amendments, the process document shedding
  three sections
- **Depends on:** [01-0011.0010](./01-0011.0010-mechanism-declared.md) (the shape, and the check
  that a declaration is true). ~~[01-0011.0020](./01-0011.0020-rules-one-home.md) (the injector,
  whose first rule targets this skill)~~ — not a dependency: the rows the injector would instruct
  read `not yet` naming it, and session nine placed this slice ahead of it *(the user, 2026-09-07)*
- **Blocks:** [01-0011.0025](./01-0011.0025-archive-duty-reaches-maintain.md),
  [01-0011.0050](./01-0011.0050-shape-checked.md) — everything after this leans on the shape, and
  this is the cheapest test of whether it survives a second application
- **Outcome:** `/maintain` is declared through the shape with its parts, its moments and its record
  obligation each carrying a state, the declaration passes the check `01-0011.0010` shipped, and
  what the second application breaks is amended in the shape rather than worked around in the
  declaration.

## Parent

[A mechanism has one home for its rules, and a derived index](../spec/01-0011-mechanism-shape.md).
`/impact`: [the 2026-09-07 re-slice assessment](../spec/01-0011-mechanism-shape.md#impact--2026-09-07).

## Why this exists, and why it is not "proves the shape generalises"

`01-0011.0010` declares `/mechanism` by its own shape. A shape that has only ever described itself
has not been tested: every choice in it was made while looking at the one thing it describes. The
second application is where a shape either survives or shows which of its rules were about the
first case all along.

This slice is placed here for **falsification, not for confidence**. Three slices —
`01-0011.0025`, `.0030` and `.0040` — build on the shape. Buying the test before they do is worth a
slice; buying it after them means amending a shape three things already depend on.

**It does not exercise injection or the record rule, and must not claim to.** ~~`/maintain` has no
records: `temporary_statements.py` reads `<temporary>` blocks in place, and there is no marks file,
no register, nothing that accumulates. So `/maintain`'s record obligation is `none` **by property**,
not deferred~~ — and the rule `01-0011.0020` installs into it has no subject here. Its first subject
is `/ticket`, one slice later.

**Falsified at this ticket's `/align`, 2026-09-07, before anything was built** *(the user)*. Life's
`maintenance` mechanism was read directly — its doc, `/maintain`, the marks library and the wake
hook. A maintenance mechanism is record-bearing **by design**: its record is the marks, one row per
mechanism per level, written only by the closing step of a maintenance, and dueness is derived from
them at every wake. The re-check rule this tree already carries as prose — *`/maintain` re-checks a
mechanism when what governs it has moved* — has no *since* without them. So the record obligation
is **`not yet`**, not `none`: the marks are the record, and the moment *knowing a re-check is due*
is instructed by nobody here. Both name the **clock ticket**,
[01-0011.0060](./01-0011.0060-mechanism-rechecked-when-governing-moves.md), minted from this
align as a child of the spec (see the decision below). Nothing in this slice builds it.

## What to build

- `.agents/mechanisms/maintain/` with its doc, declaring `/maintain`. Parts named with their
  standing: `/maintain`'s skill, `temporary_statements.py` and `test_temporary_statements.py` owned;
  `docs_corpus.py` and `tests/harness.py` **relied-on**, naming the owner — which is `/ticket`'s
  mechanism from `01-0011.0025`, and until then a declared gap. **Settled 2026-09-07** *(the
  user)*: owned — the skill, the doc, the enumerator and its tests, nothing else. Relied-on —
  the mover and its five test files, `docs_corpus.py` and `tests/harness.py`, all `/ticket`'s
  from `.0025` and a declared gap until then, since the mover goes where its instruction goes;
  `mechanisms.py`, `mechanism-shape`'s; the glossary, nobody removable. The mover's docstring,
  *the archive half of `/maintain`*, is corrected in this slice. **No trigger row**: the ring in
  `AGENTS.md` is the loop's own and the pacer's once it exists, and `/maintain` sits on it; the
  doc says it is reached from the ring and from `/verify` naming it. **`/implement` should not
  name `/maintain` at all** *(the user, 2026-09-07, at `/plan`)*: its lines 38–40 are an artefact
  of the era when `/sync-arch` was held separately, ruled a defect and routed to
  [01-0017](./01-0017-io-graph-coherent.md)'s sweep.
- A moments table covering every occasion at which a person acts on `/maintain`, each row carrying
  its instruction or which kind of absence it is. A `not yet` names a ticket that exists.
- ~~The record obligation stated as `none`, with the reason. An absence that is a property reads as
  an oversight forever unless it says so.~~ The record obligation stated as `not yet`, naming the
  clock ticket: the marks are the record this mechanism owes and does not have.
- Its evidence at `docs/mechanisms/maintain.evidence.md`, moved from
  `docs/research/maintenance-findings.md` and **sorted on the way**: what explains why the
  instruction is what it is belongs in the doc; what explains why the doc is what it is stays
  evidence. The file today is an unsorted mixture.
- [maintain/SKILL.md:138](../../.agents/skills/maintain/SKILL.md)'s link into `docs/` moved inside a
  `<project-local>` block, per `AGENTS.md`'s *Core and instance* rule. The same treatment for its
  links to `docs/process.md` and `docs/architecture.md`, which are references to documents only this
  project has.
- ~~A register row in `.agents/README.md`, in the `<project-local>` block `01-0011.0010`
  establishes.~~ **Superseded 2026-09-07 by that slice:** the register is derived, no file holds
  it, and `.agents/README.md` has no `<project-local>` block. `.agents/mechanisms/maintain/`
  existing *is* the row, and `mechanisms.py --index` renders it.
- **Added at the align, 2026-09-07**, each owed by a decision below: `/maintain`'s body rewritten
  as the grouped, addressable rules A1–E3, stating the four things once and the interim dueness
  rule inside a `<temporary until="01-0011.0060 is done">` block bound to
  [that ticket](./01-0011.0060-mechanism-rechecked-when-governing-moves.md); `.agents/glossary.md` gaining **Documentation**, **Implementation** and **Record** as
  roles, and **Working rule** beside **Strict rule**; `/mechanism`'s body amended twice — *injected
  pointers* replaced by installed text, and the record obligation of a maintenance mechanism no
  longer assumed absent — with both causes in `mechanism-shape.evidence.md`; `process.md:87` and
  `:89` moved into `/mechanism`'s body as meta-rules; and `move_doc.py`'s docstring corrected to
  `/ticket`'s ownership. **Added at `/plan`, 2026-09-07:** `docs/process.md`'s *Current documents
  and evidence* goes too — session nine routed it here as a duplicate of the doc-to-evidence
  chain `/mechanism` owns, and its five citers are repaired with the other anchors.

- **`/maintain`'s own policy consolidates into `/maintain`** *(the user, 2026-09-07)*.
  [Tree maintenance](../process.md#tree-maintenance) and
  [Mechanical maintenance](../process.md#mechanical-maintenance) are rules for one mechanism,
  sitting in the half a recipient replaces. Only `/maintain` cites the first; `/impact` cites the
  second as a pointer, which is a citation and not a second home. Split by the shape's own cut:
  what is read *in the work it governs* goes to the skill, why it is that way goes to the doc.
  Until it moves, a recipient receives `/maintain`'s body **without** the responsibilities its
  policy assigns it. **Amended at the align** *(the user, 2026-09-07)*: not all of it is
  `/maintain`'s. Lines 87 and 89 — *designed from inception for mechanical derivation* and
  *anything mechanical is maintained mechanically, history included* — are `/mechanism`'s
  meta-rules and land in its body; `/impact`'s citation of the section then points at
  `/mechanism`, or leaves, which is [01-0017](./01-0017-io-graph-coherent.md)'s call. Lines 75
  to 83 and 91 to 93 are `/maintain`'s and land as the grouped rules below.

- **The nine responsibilities, including the ones `/denoise` carried.** `process.md:75` assigns
  `/maintain` architecture/code consistency in both directions, reconstruction of load-bearing
  contract shape, document consistency and canonical ownership, prose and inline-comment cleanup,
  concern disposition, records, indexes, links and archive lifecycle. **The body instructs roughly
  three of them.** Each survives into the skill with an instruction, or becomes a moment with a
  declared absence. Measured 2026-09-07, when a `/maintain` pass followed the body faithfully and
  still missed a stale roster: three of its steps have scripts that print something and one is
  judgment that prints nothing, so the unscripted step read as done. **Each one's disposition, as
  aligned** *(the user, 2026-09-07)*: architecture/code consistency and reconstruction of contract
  shape → the second of the four things, instructed in the re-check row at drift scope,
  `elsewhere` naming `/verify` at landing; document consistency and canonical ownership → the
  fourth thing, instructed; prose cleanup → instructed; inline-comment cleanup → `elsewhere`,
  `/verify`; concern disposition → `not yet`, [01-0017](./01-0017-io-graph-coherent.md); records
  → `not yet`, [.0020](./01-0011.0020-rules-one-home.md) and the maintainer script of
  [.0025](./01-0011.0025-archive-duty-reaches-maintain.md); indexes → by hand until
  [.0040](./01-0011.0040-queue-derived-index.md), the mechanism index derived; links outside a
  close → `not yet`, [01-0017](./01-0017-io-graph-coherent.md); archive lifecycle → `embedded`,
  `/ticket`'s until [.0025](./01-0011.0025-archive-duty-reaches-maintain.md) installs it.

- ~~**Mechanism, documentation and code are three subjects, not one** *(the user, 2026-09-07)*. A
  pass over a mechanism validates that core and mechanism rules still apply and that evidence is
  extracted to its own home; a pass over documentation checks canonical ownership; a pass over code
  is neither. The distinction is **core** and belongs in the skill — the body says "code" zero
  times today. Which of the three a given project actually has is **not** core; see the decision
  below.~~ **Superseded the same evening, at this ticket's `/align`** *(the user, 2026-09-07)*,
  after Life's `/sync-arch` was read in its general form — *hold an implementation to its
  documentation* — and applied to this repository, whose implementation **is** `.agents/`. The
  three subjects were one activity seen from three sides. **`/maintain` holds four things in
  agreement:**
  1. Docs apply the meta-rules and take their format. A script rules on the format; the rest is
     judgment. When the meta-rules move, every doc under them is due.
  2. Docs and what they describe agree, **both ways** — the implementation may ignore what the doc
     declares, or the doc may not record a decision the implementation made. Either side can be the
     wrong one; a contradiction needing a new decision goes to `/align`.
  3. Live records follow their declared format, checked by a maintainer script — format never
     content, archived history exempt.
  4. Every fact has one home across docs — what remains of `/denoise`.
  Items 1 and 2 apply to two kinds of doc: **mechanism docs against skills and scripts**, which
  every project has, and **architecture against code**, which only a project with a product has.
  Item 3 is the harness's records — tickets, RFCs, sessions, marks — which every project has,
  since the mechanisms that write them travel; a product's data is the product's own. Item 4 is
  every doc. At landing, items 1 and 2 over the landed slice are **`/verify`'s** — `process.md:54`
  already gives it *match, leakage and doc-caused weirdness*; `/maintain`'s occasion is drift, a
  governing side that moved with no landing behind it, or a sum of clean landings that no longer
  agrees. The first specimen is this slice's own: `mechanism-shape`'s doc declares `/maintain`
  instructs re-checks and `/maintain`'s body does not — item 2, harness kind, implementation
  ignoring the doc. **Core must say what plays each role** *(the user)*: what counts as
  documentation, implementation, record and meta-rule is the method's vocabulary and goes in
  `.agents/glossary.md`, with the four things stated once in `/maintain`'s body; **which** files
  play them in a given project — one pair here, two in a code project — is the facet below.

- **Concern disposition is a moment with no instruction.** `process.md:75` assigns it; the body
  never mentions concerns; and `docs/concerns.md`, which six installed skills transact against, has
  never existed. The moments table carries it as `not yet` naming
  [01-0017](./01-0017-io-graph-coherent.md), which already owns that file.
- Whatever the second application breaks, amended **in `/mechanism`'s body**, with what broke it
  recorded in `docs/mechanisms/mechanism-shape.evidence.md`. A shape bent to fit a declaration is
  the failure this slice exists to catch.

## Decisions this ticket's align owns

- ~~**`/maintain`'s moments, and which absences are honest.** The taxonomy is only worth having if
  the second application is as honest as the first; a moment nobody acts on is not a moment and
  the row does not exist.~~ **Settled 2026-09-07** *(the user)*, derived from the nine
  responsibilities and from every sibling skill that hands work to `/maintain`. Instructed by the
  body: declaring a scope and running the pass; deciding whether a `<temporary>` condition holds
  and retiring the block; closing a ticket and its RFC (the permission form — the duty arrives with
  [.0025](./01-0011.0025-archive-duty-reaches-maintain.md)); updating the header and queue row by
  hand until [.0040](./01-0011.0040-queue-derived-index.md); cleaning prose and records to one
  home; running the verification set the scope touched; and — new in this slice — **re-checking a
  mechanism when what governs it moved**, in Life's order, which `mechanism-shape`'s doc already
  claims `/maintain` instructs and the body does not yet honour. `not yet`: knowing a re-check is
  due ([.0060](./01-0011.0060-mechanism-rechecked-when-governing-moves.md)); checking records
  against their declared shape
  ([.0020](./01-0011.0020-rules-one-home.md), whose first injected rule is exactly this);
  disposing of a concern ([01-0017](./01-0017-io-graph-coherent.md)); **checking links outside a
  close** — the only link check is the mover's note over records a close rewrote, and session nine's
  *zero broken links* was counted by hand — also [01-0017](./01-0017-io-graph-coherent.md), whose
  outcome already says documents referenced either exist or stop being referenced, widened by one
  sentence rather than minting a ticket for forty lines over `docs_corpus.py`; and installing or
  removing this mechanism ([01-0010](./01-0010-dev-harness-shared-and-local.md)). `unowned by
  design`: picking up a dream — `AGENTS.md` says *may*, which is a permission and not a duty, while
  `/dream` is experimental. **Not moments of `/maintain` at all** *(the user)*: **committing** what
  a pass changed — a person acts on the repository, not on this mechanism, so the body's
  *read `commit` and `push` before staging* is a pointer at another skill's domain and leaves;
  and **cleaning inline comments**, which `process.md:75` assigned here and which is `/verify`'s —
  `/verify` already applies `/improve-comments` in scope. The row reads `elsewhere`, naming
  `/verify`, so a reader sent here by the old assignment finds where it went.
  **Two rows are `embedded`, not instructed** *(the user, 2026-09-07)*: closing a ticket and
  its RFC, and updating the header and queue row, are instructions `/ticket` owns — its paired
  close and its record shape — sitting in `/maintain`'s body until
  [.0025](./01-0011.0025-archive-duty-reaches-maintain.md) installs them from `ticket.rules.md`.
  Repairing mechanically is **not** a third: D1 and D2 are meta-rules, and meta-rules are never
  injected, so that row is `elsewhere`, naming `/mechanism`. `embedded` is the kind the first
  declaration never used, so the second application exercises all four. The same reading holds
  across the corpus — `/verify`'s *anything mechanical must be*, its concern-index sweep, the
  repair policy restated in two skills, and possibly `/implement`'s *do not move the RFC*, which
  the user has not ruled on — and sweeping it is [01-0017](./01-0017-io-graph-coherent.md)'s, with
  [01-0016](./01-0016-responsibility-coherent.md) dissolved by it as the spec foresaw.
- ~~**Whether `/maintain` is always-on.** Nothing can uninstall the thing that maintains, which is
  the argument that made `/mechanism` always-on — but `/maintain` is not the definition of
  installing, so the argument may not carry. It decides whether the doc owes lifecycle pieces.~~
  **`installed`** *(the user, 2026-09-07)*. The format defines `always on` as *nothing can install
  or uninstall it*, which is true of the thing that defines installing and false of `/maintain`: a
  recipient can delete its skill and its enumerator and the ring still runs, unmaintained. Writing
  `always on` would have been a label the format's own definition contradicts. Consequences: the
  parts table reads as what an installer adds and an uninstaller removes; installing and
  uninstalling are two moments, both `not yet`, naming the parent
  [01-0010](./01-0010-dev-harness-shared-and-local.md), which owns distribution; and the doc says
  what removal costs. Life's own `maintenance` doc, read the same day, carries `State: installed`
  and the words *always on* in the next paragraph — the same confusion, unresolved there.
- **The clock is its own ticket, not this slice's** *(the user, 2026-09-07)*. Building it here
  would make `/maintain` the first record-bearing mechanism ahead of `.0025`, which was chosen for
  that role because `/ticket`'s records already exist and only need a shape declared; it would need
  a record shape, a maintainer script, a hash library and a mark writer; and this tree has no wake
  surface to announce dueness from until [01-0010.0120](./01-0010.0120-host-delivery-surfaces.md)
  lands, so the clock could only be a `/verify`-time command — the delivery Life measured at ~8%
  when a rule fires at a moment instead of reading state at wake. With two mechanisms declared,
  *core moved, so every instance is due* is two docs to reread; the recursion earns its machinery
  once [01-0017](./01-0017-io-graph-coherent.md) declares the rest. What this slice takes from Life
  without the clock is the **order of the mechanism subject** in `/maintain`'s body: `/mechanism`,
  then the instance's doc and rules, then its records, each governing the next, and never stopping
  at the step the pass was sent for. **To mint through `/ticket` after this align**, as a child of
  the spec: *a mechanism is re-checked when what governs it moves* — Life's two levels whole,
  `rules` and `output`; governing surface `/mechanism`'s skill directory and nothing else, since
  `AGENTS.md` changes for reasons that govern no mechanism; a marks record with its maintainer
  script; dueness derived at every look and never stored. After `.0025`, wanting `.0120`'s answer
  on where its announcement lands. Doc-to-code is **not** a third level: in Life it is `/sync-arch`,
  clockless, and here it is already the code subject among `/maintain`'s three.
- ~~**The authority on each of its rules** — who may change it, at what strength.~~ **Settled
  2026-09-07** *(the user)*. ~~Two strengths: a **strict rule** carries the user's name and date and
  changes only through `/align` with them; a **working rule** was written by a session under the
  repair policy or came with a selected skill, and a maintainer may amend it under
  repair-and-report, recording the cause in the evidence. Each rule carries its provenance inline.~~
  **Reversed at `/implement`, 2026-09-07** *(the user: "how does it help the executing agent?")*.
  It does not. Strength and attribution serve the maintainer at amend time, and the repair policy
  already decides who may amend what — a clear violation of an explicit rule is repaired, a new
  decision goes to `/align` — so a second label duplicated it. The body carries rules by ID as
  bare instructions, per `/skill-up`'s *instruction, not story*; the doc carries who decided
  each rule in one table; **Working rule** left the glossary. The strength column below is
  history.
  **Four more, the same hour** *(the user)*: (a) what other mechanisms will install is
  **demarcated now** — one block per owner, hand-copied inside a `<temporary>` bound to the
  ticket that installs it, so the enumerator lists it until then; this supersedes *no prose
  substitute* for the maintainer-script rule, whose row reads `embedded`; (b) the body **names no
  other skill except `/align`** — what is not this mechanism's is stated as what it does not do,
  never as who does it instead, and the doc's table names the other party; (c) B3 and C1 are
  **generic** — archive every finished record, decide finished yourself, the record's format says
  what finished means — so `/maintain` owns them and only paired close's specifics are the ticket
  mechanism's block; (d) one command example for temporary statements, not two, and the
  description narrates use, not the enumerator.
  The rules, grouped by responsibility, one per row, restatements removed — the grouping is also
  the body's section order:

  **A. What is held, and when.** A1 *(strict, the user 2026-09-07; ownership from
  [01-0010:40](./01-0010-dev-harness-shared-and-local.md), 2026-09-05)*: docs are derived work of
  the meta-rules; docs and their implementation agree both ways; live records are derived work of
  their declared format; one home per fact. A2 *(strict, the user 2026-09-07)*: at landing the
  agreement over the landed slice is `/verify`'s; `/maintain`'s occasion is drift. A3 *(working,
  [process.md:91](../process.md))*: when a governing side moves, what it governs is due, and
  governing surfaces are repaired before their derived work. A4 *(strict, the user 2026-09-07)*:
  dueness is read from the clock, never inferred — and **until the clock lands, a `<temporary>`
  block bound to [.0060](./01-0011.0060-mechanism-rechecked-when-governing-moves.md) says every
  declared mechanism in scope is due at every pass**,
  so the gap is loud in the enumerator rather than silent in the agent.

  **B. Scope.** B1 *(strict, the user 2026-09-06, [0070](./done/01-0010.0070-install-maintain.md))*:
  a pass declares its scope — the tree, a project, or one ticket's work with its consumers and
  governing docs. B2 *(working, [process.md:77](../process.md), :91)*: a pass certifies only what
  it examined and marks nothing checked that was not; **a finding outside the scope is captured and
  routed to the record that owns its subject, after searching the open issues so one already held
  is cited and not written again** *(the user, 2026-09-07)* — the open issue that holds it, the
  owning ticket's open decisions, `/align` if it needs a decision, the concern index once
  [01-0017](./01-0017-io-graph-coherent.md) gives it existence; who owns capture and routing as a
  mechanism stays [01-0010:41](./01-0010-dev-harness-shared-and-local.md)'s. B3 *(working, the
  assistant 2026-09-07, `b7a961b`)*: every pass moves every finished ticket and RFC to `done/`,
  whatever scope it declared — finished is visible from the header alone.

  **C. Judgment is the maintainer's; scripts rule on form.** C1 *(strict, the user 2026-09-06,
  [01-0010:33](./01-0010-dev-harness-shared-and-local.md))*: a ticket is finished when every
  acceptance box is checked; the mover only moves. **Finished is a mechanical read** *(the user,
  2026-09-07)* — [.0030](./01-0011.0030-archive-backlog-listed.md)'s listing is the read half and
  `move_doc.py` the write half, so under D1 the close is those two commands and the maintainer's
  act is running them; whether a checked box is *true* is `/verify`'s, not this pass's. C2
  *(working, [architecture](../architecture.md#temporary-statements))*: whether a temporary
  statement's condition holds is judged from evidence; the tool never interprets an `until`. C3
  *(working, same)*: what outlives a removed statement is rehomed first, children before parent. C4
  *(strict, the user 2026-09-07, [spec](../spec/01-0011-mechanism-shape.md))*: a **maintainer
  script** — one per record-bearing mechanism, slug in its filename, none existing yet and the
  first being [.0025](./01-0011.0025-archive-duty-reaches-maintain.md)'s — checks format never
  content, live rows only. **The rule reaches `/maintain` by injection, never by a filename
  pointer** *(the user, 2026-09-07)*: a rule reachable only through a link in another skill is the
  glossary's third tier, a diagnosis and not a home, and a body carrying many such links is read
  with them omitted. That is what [.0020](./01-0011.0020-rules-one-home.md) installs, its first
  proven rule being exactly this one; until then the row is `not yet`, and the body carries no
  substitute pointer. Story in a *doc* is A1's first item failing and is judgment under the re-check
  row; story in a *ticket* is by design.

  **D. Mechanical repair.** D1 *(strict, the user 2026-09-05,
  [01-0010:28](./01-0010-dev-harness-shared-and-local.md))*: what can be maintained mechanically is,
  and a missing script is written as part of the repair. D2 *(strict, same)*: archived records are
  not exempt. **D1 and D2 are `/mechanism`'s meta-rules, not `/maintain`'s** *(the user,
  2026-09-07)*: they govern how every mechanism is built and repaired, and `/mechanism` already
  carries their consequence, *where a record-bearing mechanism has no maintainer script, writing
  it is the maintenance*. Their home is `/mechanism`'s body, with
  [process.md:87](../process.md) — *designed from inception for mechanical derivation* — going
  with them as the incept-time form of the same rule. **Meta-rules are never injected** *(the user,
  2026-09-07)*: they live in `/mechanism` or in the entry file's core rules, and a skill applying
  one reads it there. So the body's copy leaves, the moments row *repairing mechanically* reads
  `elsewhere`, naming `/mechanism`, and `/verify`'s own copy at
  [verify/SKILL.md:38](../../.agents/skills/verify/SKILL.md) is the same defect one skill over —
  routed to [01-0017](./01-0017-io-graph-coherent.md), not repaired here. D3 *(working, [process.md:93](../process.md))*: a repair of an archived record
  preserves the facts it records and never implies a later requirement was met at the time. D4
  *(strict, the user 2026-09-06, [0070](./done/01-0010.0070-install-maintain.md))*: a mover run
  that stops partway is unfinished maintenance — inspect, finish under the repair policy, never
  report a close that did not finish; the mover's own contract stays in
  [architecture](../architecture.md#interruption-and-recovery).

  **E. One home per fact.** E1 *(working, `/denoise` as selected)*: a restatement becomes a
  pointer; a summary pointing deeper is not a duplicate. E2: live tickets, RFCs and session
  records may carry duplicate context; their indexes may not. E3: core documents do not cite them
  for architecture. **Shorter is better** *(the user)*: every rule lands in the body at the length
  above or less. **The body is written as addressable rules** *(the user, 2026-09-07)* — the
  groups and IDs above, one rule per line — as the first instance of a skill structure in which
  a rule can be named by an injector's anchor; the structure itself, and a check that a
  mechanism's skill is an instruction set rather than prose, are recorded as `/skill-up`'s owed
  change on [01-0010.0100](./01-0010.0100-remaining-named-corpus.md). One shape, no
  generalisation here.
  **Installed text, not pointers** *(the user, 2026-09-07)*: `/mechanism`'s body says both
  *injected pointers* and *writes its rules into the targets*; the second holds. A rules file is
  a rule's only authored home, every reader carries an installed block — `/mechanism` itself
  included, when it reads its own rule in its own work — and no body restates one. The seven
  rules `mechanism-shape.rules.md` will hold, and their two targets, are recorded as
  [.0020](./01-0011.0020-rules-one-home.md)'s input; here they fix which rows read `not yet`
  naming it: records against their shape, the re-check's comparison rule, and story-in-a-doc,
  with no prose substitute in the body until the block lands. This is the second amendment the
  second application forces on the shape, to be made in `/mechanism`'s body with its cause in
  `mechanism-shape.evidence.md`.

- **Where "this project's code is entirely mechanism parts" is written down** *(raised by the user,
  2026-09-07)*. In this repository there is no product code: every file under `.agents/scripts/`
  and `tests/` is a part of some mechanism, so code-maintenance and mechanism-maintenance coincide
  here and nowhere else. **Recommendation: it is a facet, not a `<project-local>` block.** A block
  in every skill that touches code is N copies of one fact, which is the defect this spec exists to
  stop; [01-0010.0110](./01-0010.0110-project-facets-injected.md) already owns *a project answers
  the harness's questions about its own facets once, and those answers are installed into the
  places that need them*. This align decides whether that holds, and if so the facet is recorded
  there rather than built here. **Holds, restated** *(the user, 2026-09-07)*: the facet is no
  longer *which of mechanism, documentation, code does this project have* but **which
  documentation-and-implementation pairs it has**. This project's answer is one row, mechanism docs
  against `.agents/`; a code project's is two, the second being architecture against its code. A
  list of pairs is what `/maintain` iterates over, and it replaces a three-way switch with the
  thing the switch was standing in for. Recorded on
  [01-0010.0110](./01-0010.0110-project-facets-injected.md); nothing local is written here.

- **Who grades this slice, pre-registered** *(the user, 2026-09-07)*. Two graders, two
  questions. **The user** grades the shape amendments — *installed text* replacing *injected
  pointers*, and the record obligation moving from `none by property` to `not yet`: did the
  shape change because `/maintain` did not fit it, or was `/maintain`'s declaration bent to fit
  the shape? A declaration hand-fixed until the check went quiet reads identically to success.
  **The session that declares `/ticket`** at [.0025](./01-0011.0025-archive-duty-reaches-maintain.md)
  grades the declaration, as the first to lean on it: it takes the mover and `docs_corpus.py` as
  its own, installs the paired-close block into `/maintain`, and retires the two `embedded` rows
  — did those land against `/maintain`'s doc as written, or did the doc have to be rewritten to
  receive them? **What retires `/maintain`**: a tree in which every one of the four agreements is
  held by a script in the verification set, leaving a pass no judgment to apply; until then
  `/maintain` is the judgment half and the scripts are its parts.

## Acceptance criteria

- [x] `.agents/mechanisms/maintain/` holds a doc naming every part with its standing, and every
  relied-on part names an owner or a declared gap with a ticket. **2026-09-07:** five owned parts,
  ten relied-on rows, the mover and its five test files, `docs_corpus.py` and `tests/harness.py`
  naming `.0025`'s gap; the check reports every path resolved.
- [x] The moments table covers every moment, each row carrying an instruction or a kind of absence;
  every `not yet` names a ticket that exists. **2026-09-07:** sixteen rows — six instructed, three
  `embedded`, three `elsewhere`, three `not yet`, one `unowned by design`; the check reports every
  referent resolved. The moment about what an inceptor hands `/maintain` sits on
  `mechanism-shape`'s table, where the occasion is.
- [x] ~~The record obligation reads `none` with its reason, distinguishable from unstated.~~ The
  record obligation reads `not yet`, naming
  [.0060](./01-0011.0060-mechanism-rechecked-when-governing-moves.md), and the moments table carries
  *knowing a re-check is due* as `not yet` naming the same ticket. **2026-09-07:** both in the doc.
- [x] `docs/mechanisms/maintain.evidence.md` exists and `docs/research/maintenance-findings.md` no
  longer holds the mechanism's story; doc material has moved into the doc rather than travelling
  with the evidence. **2026-09-07:** moved through `move_doc.py`, five citers repaired by it; the
  two probe records stayed as evidence and a section on what the align refuted was added. Nothing
  in the old file explained the instruction, so nothing moved to the doc.
- [x] No link from `.agents/skills/maintain/` into `docs/` sits outside a `<project-local>` block.
  **2026-09-07:** the body's `<project-local>` holds the verification set and two architecture
  contract sections; outside it, `docs/` appears only in the `<temporary>` binding, which the
  entry file exempts, and in the command examples, which name path conventions.
- [x] `docs/process.md`'s *Tree maintenance* and *Mechanical maintenance* are gone, their rules in
  `/maintain`'s skill and their reasons in its doc — except lines 87 and 89, which are
  `/mechanism`'s meta-rules and land in its body, ~~`/maintain` carrying them as `embedded` until
  the injector~~ read there, since meta-rules are never injected; the `<temporary>` block bound
  to this ticket is retired with them, and `temporary_statements.py` reports one fewer.
  **2026-09-07:** the block removed through `--remove` with its fingerprint; the three sections
  and *Current documents and evidence* deleted; line 21 points at the declaration; the enumerator
  reports five statements — the pacer's, the entry file's, and three in the skill body: the
  clock's interim rule and the two hand-copied blocks — where the process document's is gone. Twenty-two anchor citations in eight records repaired by hand to
  the three new homes; two prose mentions of the old evidence path in `done/0090` and the queue's
  history left as recorded facts.
- [x] `/maintain`'s body instructs, or declares an absence for, each of the nine responsibilities
  `process.md:75` assigned it — concern disposition included, as `not yet` naming
  [01-0017](./01-0017-io-graph-coherent.md). **2026-09-07:** per the disposition list above.
- [x] ~~The body distinguishes maintaining a mechanism from maintaining documentation from
  maintaining code, and says what each obliges.~~ The body states the four things `/maintain`
  holds in agreement, once, and says that landing-time agreement is `/verify`'s;
  `.agents/glossary.md` defines documentation, implementation and record as roles, so that a
  project's pairs can be named in those words. **2026-09-07:** A1 and A2; four glossary entries,
  with *entry* left unreserved because core already uses it for a row of a record.
- [x] `mechanisms.py --index` renders a `maintain` row from `.agents/mechanisms/maintain/`. No file
  holds a register. **2026-09-07:** rendered, two rows.
- [x] The check from `01-0011.0010` runs against two mechanisms and reports both declarations true.
  **2026-09-07: both true, and the check did not change** — the three amendments were to
  `/mechanism`'s prose and the format shelf, and `mechanisms.py` is byte-identical to before this
  slice. Three tests were added over paths the first declaration never exercised, each watched
  failing under a deliberate break; 112 → 115.
- [x] Every amendment the second application forced is in `/mechanism`'s body, with its cause in
  `mechanism-shape.evidence.md` — or the ticket records that the shape needed none, which is a
  result rather than an omission. **Three are known at `/plan`:** installed text, not pointers;
  D1 and D2 as meta-rules in the body; and *a sentence about another mechanism is that
  mechanism's* — installed where it sits, never authored there. The first and third landed in the
  body at `/plan` on the user's instruction, 2026-09-07, with their evidence; the sentence about
  `/maintain` is therefore `maintain.rules.md`'s, uninstalled until `.0020`. **2026-09-07:** the
  second landed as *Mechanical by construction*; `MECHANISM-FORMAT` gained the one-code-span
  rule its parser already enforced, which is the format shelf's own docs-agree-with-implementation
  repair.
- [ ] `/verify` has been run on this ticket against its ticket, RFC, and governing docs.

## Out of scope

Declaring `/ticket` ([0025](./01-0011.0025-archive-duty-reaches-maintain.md)), which is the third
mechanism and the first with records. Building a maintainer script — `/maintain` has no records to
maintain.

**`docs/process.md`'s *Autonomy and repair* stays put, and the reason is now evidenced**
*(the user, 2026-09-07)*: it is **shared**, not `/maintain`'s. `/maintain` and `/verify` both cite
`#autonomy-and-repair`, `/implement` defers to the `repair` switch, and the switches live in
`AGENTS.md`. [01-0016](./01-0016-responsibility-coherent.md) owns where it lands. *Verification*
stays too — it is a project facet, [01-0010.0110](./01-0010.0110-project-facets-injected.md)'s.

**Corrected 2026-09-07:** *Mechanisms and skills* was listed here as out of scope on the ground
that it belonged to a mechanism nobody had declared. That ground expires exactly when this ticket
runs, and `01-0011.0010` bound the section's surviving sentence with
`<temporary until="01-0011.0022 is done">`. A statement cannot name an owner who has written down
that it is not theirs. It is in scope, above. Declaring any mechanism beyond the three the spec scopes — [01-0017](./01-0017-io-graph-coherent.md)
applies the shape to the rest.

**A mechanism declares what `/maintain` must do for it, and this slice lands the instruction**
*(the user, 2026-09-07, at `/plan`)*. Maintenance is one instance of the general seam of
mechanism construction — what a mechanism asks of other mechanisms — and the mechanism doc is
already that record, so this is not an Edge. ~~The instruction lands as a sentence in
`/mechanism`'s *Incept*.~~ **Corrected the same hour** *(the user)*: **a sentence about another
mechanism is that mechanism's** — what shapes how mechanisms are built around it is a rule in
its rules file, installed where it sits and never authored there, while its core function
reaches a session through its own description, strengthened at tier 1 where warranted. That
general rule is in `/mechanism`'s body now. The sentence about `/maintain` is therefore
`maintain.rules.md`'s, one rule targeting `/mechanism`'s *Incept*, authored in this slice and
uninstalled until [.0020](./01-0011.0020-rules-one-home.md) — Life's arrangement exactly, its
`maintenance` block sitting inside `/mechanism`. First use by a mechanism:
[.0025](./01-0011.0025-archive-duty-reaches-maintain.md). The map of every missing instruction
across the corpus is [.0050](./01-0011.0050-shape-checked.md)'s report, its homes
[01-0017](./01-0017-io-graph-coherent.md)'s.

## Parent scope addressed

Stories 14, 19, 20, 24.
