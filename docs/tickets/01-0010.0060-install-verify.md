# Install /verify

- **Status:** Done (2026-09-06)
- **Type:** HITL
- **Plan:** [Install /verify — implementation plan](../rfc/01-0010.0060-install-verify.md) — install `/verify`, first use reviews 0035
- **Depends on:** [Install /spec](./01-0010.0035-install-spec.md) (the spec and `/spec`)
- **Outcome:** `/verify` is installed from the accepted selection, discoverable, and used once as the verification of a landed harness slice — ticket, RFC, governing docs, and the project's verification set — under repair-and-report.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). Loop-completion slice from [the spec](../spec/01-0010-dev-harness-shared-and-local.md#harness-loop-on-itself--agreed-2026-09-06). `/impact`: [Delivery-ring breakdown](../spec/01-0010-dev-harness-shared-and-local.md#delivery-ring-breakdown--2026-09-06).

## What to build

- Meteoscape `review-impl` installed as `/verify`, reconciled with [repair-and-report](../process.md#autonomy-and-repair). `/verify` is the verification of landed work, not only documentation or shape review. The check set is [Verification](../process.md#verification), linked, not inlined. Callers use `/verify` and `/plan`, not `/review-impl` or `/plan-impl`.
- First use: verify a landed slice — [01-0010.0020](./01-0010.0020-live-alignment-across-hosts.md),
  [01-0010.0030](./01-0010.0030-install-ticket.md),
  [01-0010.0035](./01-0010.0035-install-spec.md), or
  [01-0010.0040](./done/01-0010.0040-install-plan.md) — against its ticket, RFC (if any)
  and governing docs. Installing `/verify` does not wait on `/implement`.

## Acceptance criteria

- [x] `/verify` is under `.agents/skills/verify/`, and a fresh session lists it the same way `/spec` is listed. **2026-09-06, Cursor:** `.claude/skills/verify/SKILL.md` and `.cursor/skills/verify/SKILL.md` resolve through the existing links, the same loader path that listed `/spec`.
- [x] `/verify` is used once to verify a landed harness slice; repair-and-report holds. **2026-09-06:** first use on [01-0010.0035](./01-0010.0035-install-spec.md). Match; no repair on that slice.
- [x] Installed files name `/verify` and `/plan`; they do not name `/review-impl` or `/plan-impl`. **2026-09-06:** grep of `.agents` is clean.
- [x] `AGENTS.md`'s local block lists `/verify` among the installed skills. **2026-09-06:** `Installed: /align, /impact, /ticket, /spec, /plan, /verify`.
- [x] `/verify` has been run on this ticket against its ticket, RFC, and governing docs. **2026-09-06:** this install. Match: `name: verify`, repair-and-report, callers `/verify` and `/plan`. RFC stage 3 had leaked the `done/` move into this slice; amended — close is TICKET-FORMAT and `/maintain`. This ticket is not moved to `done/` in this pass. **Same day, user:** restore the source `/improve-comments` cite; omitting it was the crop. Body now names `/improve-comments`. The skill folder is 0050's. **Same day, user:** `/verify` is the whole verification of landed work; the suite is linked from [Verification](../process.md#verification), not inlined in `/implement`.

## Out of scope

Installing `/maintain`. That is the next slice. Installing `/implement` is not a prerequisite. `/improve-comments` is cited; 0050 installs it.

## Parent scope addressed

Story 11; selection `/verify`; repair-and-report.
