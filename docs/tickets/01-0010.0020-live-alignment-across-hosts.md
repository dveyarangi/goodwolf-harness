# Align on harness work in Codex, Cursor and Claude Code

- **Status:** Done (2026-09-06)
- **Type:** HITL
- **Depends on:** [Shared harness across projects](./01-0010-dev-harness-shared-and-local.md) (the accepted source selection and the current process policy; not its completion)
- **Outcome:** The user can open this repository in any of the three hosts and align on its current work using the same installed `/align` and `/impact`, current decisions and glossary, with evidence of which skills each host actually made available.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). This is its first delivery slice: spec stories 5, 6, 8 and 10, narrowly for live alignment and its impact assessment. The parent keeps the remaining corpus, `/maintain`, and cross-project distribution.

## What to build

One complete alignment path: project entry → the applicable Tier 1 rules and skill descriptions → the installed `/align` and `/impact` with their format shelf → current project records → a concrete decision recommendation recorded in the existing owner. No pacer routing is needed for this slice. `/impact` uses its selected method and the agreed history policy; it gains no automatic spec gate.

The installed files are the recipient layout: `.agents/skills/` holds the bodies, `.claude/skills` and `.cursor/skills` link to it, Codex reads it natively → [.agents/README.md](../../.agents/README.md).

## Decisions this ticket's align owns

- ~~The root from which this project's three host sessions operate and where the working corpus lives.~~ **2026-09-05, user: `D:\Dev\AI\agents`, a git repository. The working records moved there with the tree; the installed corpus is `.agents/skills` inside it; the July corpus is evidence under `legacy/skills`.**
- The minimum Tier 1 entry contract for this slice: which already-agreed rules and descriptions must arrive at session start, how the detailed current records are reached from them, and what observed evidence establishes availability. Host-specific file and hook implementation belongs in the RFC that follows.
  - ~~Where does the entry contract live?~~ **2026-09-05, user: one root `AGENTS.md` owns the content; `CLAUDE.md` is the single line `@AGENTS.md`; no Cursor rule file.** Codex and Cursor read `AGENTS.md` natively, Claude Code through the import. One owner, no drift, no generator. Cursor's `AGENTS.md` support and Claude Code's import are the load-bearing assumptions; each host trial checks its own.
  - ~~What does `AGENTS.md` carry?~~ **2026-09-05, user: the loop as a stage/skill/checkpoint table, the autonomy switches with their current values, the Tier 2 pointers and the canary. `docs/process.md` keeps only the detail behind them and points at the root for the loop.** Skill names in the loop: `/spec` (was `/to-spec`) and `/verify` (was `/review-impl`), decided in the same pass. Roster, **2026-09-05, user:** `commit`=ask, `push`=ask, `next-cycle`=ask, `breakdown`=ask, `repair`=report. `history` and `decide` were proposed and dropped: a rule with one value is a rule, not a switch, and deciding is always `/align`. `repair` stays because it is the one trust level, not a permission gate; `ask` is its early-project position.
  - ~~What is the evidence that the contract arrived?~~ **2026-09-05, user: a versioned line `Entry contract: vN, date` in `AGENTS.md`, bumped by hand when the contract changes, and the rule that the first reply of every session opens with that line verbatim.** A session that opens without it did not load the file; one announcing an old version runs on stale rules. The headless probe asks for the same line. No start hook: no host-equivalent exists across the three, and emission is not delivery.
  - ~~Is the entry file core or local?~~ **2026-09-05, user: both, like a skill. The core part is the announce line, the loop, the switch roster with meanings and the `<temporary>` convention, identical in every project; the `<project-local>` block holds what the project is, where its records live, and the switch values.** Values local means nothing local overrides core. The version line versions the core part only.
  - ~~Is `/impact` a stage?~~ **2026-09-05, user: no, it is called from `/align`, `/ticket` and `/plan` to size work, check a slice, and say what shape the outcome takes: spec, ticket or RFC.** The installed body does not recommend a shape yet; that extension is owed to the parent.
  - ~~How is expiring state marked?~~ **2026-09-05, user: `<temporary until="condition">`, nestable anywhere, condition testable; followed until visibly met, then reported as stale, not obeyed; `/maintain` enumerates and removes.** Second instance already existed in the align local block, which now uses it. **2026-09-06, user: every block also names the ticket whose work meets its condition, `ticket="path"` from the repository root; an unbound block is reported by `/maintain`.** The three existing blocks were bound the same day; the conditions then read as the queue the pacer paces. Entry contract bumped to v2 for the core change.
  - ~~Is the loop a stage list?~~ **2026-09-05, user: no. Two rings joined at `/align`: the session ring, wake with `/recall` → `/align` → `/conclude`, and the delivery ring, `/ticket` → `/plan` → `/implement` → `/verify` → `/maintain` → `/ticket`, entered from `/align` and returning to it whenever a decision is needed.** `/spec` and `/impact` are called from inside the rings, not stages; `/dream` runs after `/conclude` and feeds `/maintain` and `/align`, not `/recall`. Follows the Aug 31 sketch; the first table flattened it and was replaced. **2026-09-06, user:** reopened on the evidence of the first observed wake, which found no decision pending and was still sent to `/align`; kept as drawn for now, to see how it behaves. Resume state after a session break is the pacer's concern, not the loop's.

## Acceptance criteria

- [x] The entry-contract decision above is recorded in a maintained governing document before dependent implementation planning, with expected and unobservable delivery evidence distinguished. **2026-09-06:** `AGENTS.md` v1 with the announce line as the expected evidence; delivery in Codex and Cursor is the unobservable part until each is opened.
- [x] Fresh sessions in Claude Code, Codex and Cursor each discover `/align` and `/impact` from the same identifiable revision of this repository, and their required references resolve. A manual file read does not count as discovery. **2026-09-06:** all three hosts listed the installed `/align` and `/impact`. Claude Code and Cursor observed in-session; Codex reported by the user. The dangling `EDGE-FORMAT.md` cite of `to-tickets` is [01-0010.0030](./01-0010.0030-install-ticket.md)'s, not a host miss.
- [x] An explicit `/align` request and a natural-language request to discuss an unresolved harness decision both reach the installed instructions. The agent reuses an accepted decision where one applies and asks one recommended decision question where a real choice remains. **2026-09-06, Cursor:** a request to discuss whether any decisions remain and what comes next, without naming `/align`, reached the installed skill. Accepted 0020/0030 decisions and the queue's current pass were reused; the remaining choice put to the user was the next minted slice.
- [x] An `/impact` assessment on a bounded change follows actual consumers, identifies what must change or be verified, and treats history under the agreed mechanical-maintenance policy. **2026-09-06, Cursor:** ran on installing `/ticket`. Must change: skill and format, EDGE-FORMAT cite, `AGENTS.md` temporary, process naming, queue amendment rules. Must verify: catalog listing (now observed) and one minted slice. Leave alone: pacer, remaining corpus, `/impact`'s still-owed shape recommendation. Recommendation was proceed, narrowed; enacted on [01-0010.0030](./01-0010.0030-install-ticket.md).
- [x] The result of each host trial is recorded here without creating an unrelated ticket, reopening accepted decisions, or claiming the rest of the corpus is installed. A host that could not be verified leaves its criterion unchecked. **2026-09-06:** holds for every observation; the natural-language `/align` route closed the last box the same day.
- [x] `/verify` has been run on this ticket against its ticket and governing docs. **2026-09-06:** `/verify`. No RFC (none owed as a criterion). Match: `AGENTS.md` v2, `CLAUDE.md` is `@AGENTS.md`, `/align` and `/impact` with format shelf, host trials recorded. `/impact` still does not recommend spec/ticket/RFC — that remains the parent's. No repair.

## What is still missing — 2026-09-06

`/verify` has been run. Layout necessity of `.claude/skills` vs `.cursor/skills` is not a criterion.

- **Whether `.claude/skills` or `.cursor/skills` is needed at all.** Untested as a necessity; not a criterion. Cursor's catalog in this session pointed at `.claude/skills`, which does not prove `.cursor/skills` is unused. Owner: the parent, with the layout note in [`.agents/README.md`](../../.agents/README.md).

## Host observations

- **Claude Code, session start, 2026-09-06, desktop app, tree at `7e80b2d` plus the staged handoff:** a fresh session opened in this folder replied with the entry contract line, verbatim, as its first line, and its system context showed `AGENTS.md` inlined through the `CLAUDE.md` import. Start route proven for Claude Code. In the same session an explicit `/align` request reached the installed skill through the catalog; the natural-language route is still untried.
- **Cursor, session start, 2026-09-06, this repository:** a fresh session opened with the entry contract line, verbatim. The skill catalog listed `align` and `impact` with the installed descriptions; catalog paths were `.claude/skills/align` and `.claude/skills/impact`. Explicit `/align` was not requested; `/impact` ran on installing `/ticket` when asked what that install would take. **Same day, user:** Codex also lists the installed skills. **Same day, later session:** a natural-language request about remaining decisions and what comes next reached the installed `/align` body; see criterion 3.
- **Claude Code, subagent, 2026-09-05, commit `7e80b2d`:** a subagent spawned from this session, told to use no tools, replied with the entry contract line. The `CLAUDE.md` → `.md` import resolves and the content reaches a fresh agent context. Not a session start, so partial: the start route is still to be observed. It quoted the rule together with the line, which led to separating them in `AGENTS.md`.
- **Claude Code, 2026-09-05:** after the session's working directory changed to this repository, its skill catalog listed `align` and `impact` with the installed descriptions, reached through the `.claude/skills` junction. Discovery evidence only; no invocation trial yet, and whether Claude Code would also find `.agents/skills` without the link is untested.

## Known gaps at minting

- ~~`align/EDGE-FORMAT.md` cites `../to-tickets/TICKET-FORMAT.md`, which is not installed until `/ticket` lands.~~ **2026-09-06:** repaired by [01-0010.0030](./01-0010.0030-install-ticket.md) to `../ticket/TICKET-FORMAT.md`.
- ~~No `.claude/CLAUDE.md`, Cursor rule or `AGENTS.md` exists yet.~~ **2026-09-05: `AGENTS.md` and the one-line `CLAUDE.md` written; delivery untested until the headless probe runs logged in.**
- `.claude/skills` and `.cursor/skills` are tracked symlinks; Codex needs none. Which links are strictly required is untested: Cursor documents reading `.agents/skills` directly, and Claude Code may too. If both do, the link layer can go.
- The `codex` on PATH is a May 2025 npm build that predates `AGENTS.md`; the Codex trial must use the host you actually work in.
