# Align on harness work in Codex, Cursor and Claude Code

- **Status:** In progress
- **Type:** HITL
- **Depends on:** [Shared harness across projects](./01-0010-dev-harness-shared-and-local.md) (the accepted source selection and the current process policy; not its completion)
- **Outcome:** The user can open this repository in any of the three hosts and align on its current work using the same installed `/align` and `/impact`, current decisions and glossary, with evidence of which skills each host actually made available.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). This is its first delivery slice: spec stories 5, 6, 8 and 10, narrowly for live alignment and its impact assessment. The parent keeps the remaining corpus, `/maintain`, and cross-project distribution.

## What to build

One complete alignment path: project entry → the applicable Tier 1 rules and skill descriptions → the installed `/align` and `/impact` with their format shelf → current project records → a concrete decision recommendation recorded in the existing owner. No pacer routing is needed for this slice. `/impact` uses its selected method and the agreed history policy; it gains no automatic spec gate.

The installed files are the recipient layout: `.agents/skills/` holds the bodies, `.claude/skills` and `.cursor/skills` link to it, Codex reads it natively → [.agents/README.md](../../.agents/README.md).

## Decisions this ticket's align owns

- ~~The root from which this project's three host sessions operate and where the working corpus lives.~~ **2026-09-05, user: `D:\Dev\AI\agents`, a git repository. The working records moved there with the tree; the installed corpus is `.agents/skills` inside it; the July corpus is evidence under `legacy/skills`.**
- The minimum Tier 1 entry contract for this slice: which already-agreed rules and descriptions must arrive at session start, how the detailed current records are reached from them, and what observed evidence establishes availability. Host-specific file and hook implementation belongs in the RFC that follows.

## Acceptance criteria

- [ ] The entry-contract decision above is recorded in a maintained governing document before dependent implementation planning, with expected and unobservable delivery evidence distinguished.
- [ ] Fresh sessions in Claude Code, Codex and Cursor each discover `/align` and `/impact` from the same identifiable revision of this repository, and their required references resolve. A manual file read does not count as discovery.
- [ ] An explicit `/align` request and a natural-language request to discuss an unresolved harness decision both reach the installed instructions. The agent reuses an accepted decision where one applies and asks one recommended decision question where a real choice remains.
- [ ] An `/impact` assessment on a bounded change follows actual consumers, identifies what must change or be verified, and treats history under the agreed mechanical-maintenance policy.
- [ ] The result of each host trial is recorded here without creating an unrelated ticket, reopening accepted decisions, or claiming the rest of the corpus is installed. A host that could not be verified leaves its criterion unchecked.

## Host observations

- **Claude Code, 2026-09-05:** after the session's working directory changed to this repository, its skill catalog listed `align` and `impact` with the installed descriptions, reached through the `.claude/skills` junction. Discovery evidence only; no invocation trial yet, and whether Claude Code would also find `.agents/skills` without the link is untested.

## Known gaps at minting

- `align/EDGE-FORMAT.md` cites `../to-tickets/TICKET-FORMAT.md`, which is not installed until `/ticket` lands.
- No `.claude/CLAUDE.md`, Cursor rule or `AGENTS.md` exists yet; the Tier 1 contract decides what goes there.
- `.claude/skills` and `.cursor/skills` are tracked symlinks; Codex needs none. Which links are strictly required is untested: Cursor documents reading `.agents/skills` directly, and Claude Code may too. If both do, the link layer can go.
