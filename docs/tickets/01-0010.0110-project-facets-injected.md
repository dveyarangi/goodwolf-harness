# A project's local facts are answered once and installed, not written into each skill

- **Status:** Ready (planned 2026-09-20; three choices wait on the user)
- **Type:** HITL
- **Plan:** [implementation plan](../rfc/01-0010.0110-project-facets-injected.md) — `local.rules.md`
  as the injector's second source, placed last, an `overrides` bullet, the entry file anchored by
  section heading
- **Depends on:** [01-0010.0140](./done/01-0010.0140-core-stands-alone.md) (the exemption list, whose
  `<installed by="local">` row is what lets a converted block pass the check);
  [01-0011.0020](./done/01-0011.0020-rules-one-home.md) (the injector this extends — landed 2026-09-08)
- **Related:** [01-0017.0010](./01-0017.0010-terms-defined-before-they-land.md) — both write the
  first installed block into `AGENTS.md`; one anchor convention, whichever lands first;
  [01-0010.0130](./01-0010.0130-harness-installs-into-another-tree.md) — the installer that calls
  this after every deployment
- **Outcome:** A project writes every local override and answer into one file beside its entry
  file, in the format the injector already reads; the injector installs from it after every
  deployment, last, with an override naming the rule it overrides; no core file carries a
  `<project-local>` block; and this repository is the first project to do it.

## Parent

[The harness installs into a tree that is not its own](../spec/01-0010.0130-harness-installs-into-another-tree.md),
accepted 2026-09-14. This ticket is the local-rules half of that spec — *The local rules file*
under Implementation Decisions — and every decision it rests on is recorded there with its
provenance. `/impact`: [the split](../spec/01-0010.0130-harness-installs-into-another-tree.md#impact--2026-09-14-the-split).
Rewritten from decision-bearing to feature altitude 2026-09-15; the align that produced it is in
the session records.

## Why it is one ticket and not two

The injector change and the conversion of this repository's own blocks are one act because the
conversions are what prove the injector change works: a local source that installs nothing here is
untested, and blocks converted by hand without the source are the defect this ticket exists to end.
`AGENTS.md` as a target is the seam that would split it — a target no rules file has ever named —
and it stays inside because the tier-1 rule that makes the local file binding cannot land anywhere
else. If `/plan` finds this too coarse for one RFC, it splits there.

## What to build

- **One local rules file per instance**, beside the entry file and outside `.agents/`, so a
  redeploy that replaces the core directory cannot delete it. Its entries are in installed-rules
  format — the target-and-anchor grammar the mechanism rules files use — and the injector reads it
  as one more source, with install, check and retract behaving exactly as they do for a mechanism's
  file. Rules only; `commit=ask` is a rule.
- **Local blocks install last**, after every mechanism's block in the same target, so a project's
  answer is what the reader meets after core's rule. A local entry that overrides a core rule
  **names that rule's id**; one that names none is refused with the reason.
- **`AGENTS.md` is a target.** Anchor rows name it like any skill; it takes its first installed
  block. The anchor convention is shared with
  [01-0017.0010](./01-0017.0010-terms-defined-before-they-land.md) and invented once.
- **The tier-1 rule**: every local change to a rule is written in the local file. Its teeth are the
  existing check, which fails on a block that differs from its source and on a block nothing owns —
  so a rule hand-edited into a skill is a diagnostic, not a second opinion.
- **This repository converts.** Seven `<project-local>` blocks move into the local file: the three
  in `AGENTS.md` (the glossary split, the instance-half note, the five switches), the three
  mechanism docs' `evidence` bullets, and `/maintain`'s verification-set block — **which moved
  to the entry file on 2026-09-20 under [.0140](./done/01-0010.0140-core-stands-alone.md)**, beside the
  switches, wrapped on this ticket; `/verify`'s sentence *named in the entry file's local block*
  is wrapped with it, since where the set lives after conversion is this ticket's to say. The eighth —
  `MECHANISM-FORMAT`'s template line — is a mis-tag and is deleted, not moved.
- **`<project-local>` is retired.** The tag is gone from core; the entry file's *Project-local*
  section describes the local file instead; the glossary's **Project-local** and the documents that
  describe the tag as something an author writes — `MECHANISM-FORMAT`, `/mechanism`, `/skill-up` —
  say what is now true. The *Core and instance* exemption for the tag goes with it. The entry
  contract bumps *(v9 when this was written; v12 by 2026-09-20)*.
- **The file's name, its grammar beyond the rules-file format it reuses, and how the injector tells
  a local block from a mechanism's are the RFC's** *(the user, 2026-09-10)*.

## Open issues

None. Every decision is on the spec.

## Acceptance criteria

- [ ] A local rules file beside the entry file is installed, checked and retracted by the injector
      with the same three modes a mechanism's rules file gets, and retraction leaves every target
      byte-identical.
- [ ] A local block sits after every mechanism's block in the same target, in every target.
- [ ] A local entry overriding a core rule names that rule's id; one naming none is refused, and
      the refusal says which entry.
- [ ] `AGENTS.md` holds an installed block, under an anchor convention
      [01-0017.0010](./01-0017.0010-terms-defined-before-they-land.md) can reuse without change.
- [ ] The entry file says a local change to a rule goes in the local file, and
      `inject_rules.py --check` fails on rule text in a core file that no source owns.
- [ ] No `<project-local>` tag remains under `.agents/` or in `AGENTS.md`; the seven blocks'
      content reaches its targets as installed blocks; `MECHANISM-FORMAT`'s template line is gone.
- [ ] The glossary's **Project-local**, the entry file's *Project-local* section,
      `MECHANISM-FORMAT`, `/mechanism` and `/skill-up` describe the local file and not the tag, and
      the entry contract is bumped with the change recorded.
- [ ] Behavioural tests, in the shape of the existing suite, cover install, check, retract,
      ordering, id citation and its refusal, and an `AGENTS.md` target.
- [ ] The project's verification set is green with the blocks converted.
- [ ] `/verify` has been run on this ticket against its ticket, RFC, and governing docs.

## Out of scope

The installer that calls this after a deployment —
[01-0010.0130](./01-0010.0130-harness-installs-into-another-tree.md). The exemption list a
converted block must pass — [01-0010.0140](./done/01-0010.0140-core-stands-alone.md). The glossary
rule's own tier-1 block — [01-0017.0010](./01-0017.0010-terms-defined-before-they-land.md). What
ships — [01-0011.0050](./done/01-0011.0050-shape-checked.md). The injector's existing contract in
[architecture.md](../architecture.md#installed-blocks), extended by one source and not reopened.

## Parent scope addressed

Spec stories 4, 5, 6, 7, 13, 14, 15, 16 and 19.
