# The backlog five arrive with their adaptations

- **Status:** Partial (three installed 2026-09-09; no adaptation applied beyond the two below)
- **Type:** HITL
- **Depends on:** [Each skill owns its own responsibility](./01-0016-responsibility-coherent.md)
  (so five more skills inherit settled vocabulary rather than adding to the restatement)
- **Outcome:** `/conclude`, `/recall`, `/dream`, `/review-architecture` and `/setup-devops` carry the
  adaptations the selection names for them, each has been used once here, and the twenty selected
  working commands are complete with nothing left in the parent's backlog.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md), from
[the bootstrap corpus selection](./01-0010-dev-harness-shared-and-local.md#bootstrap-corpus-selection).
These are the five [01-0010.0100](./01-0010.0100-remaining-named-corpus.md) explicitly leaves to the
parent; that ticket keeps `/advise`, `/skill-up`, `/commit`, `/celebrate` and `/edge`. The parent held
them as a backlog line, not a ticket, which is why nothing tracked them.

## Three arrived early, again

**2026-09-09.** The user installed `/recall`, `/review-architecture` and `/setup-devops` directly,
alongside `/edge`, asking for the copy now and the mechanism work as tickets. `/conclude` and `/dream`
were already installed. That is a decision, not a violation — the same decision, and the same
consequence, as [01-0010.0100](./01-0010.0100-remaining-named-corpus.md#four-arrived-early-2026-09-07)
records for its four on 2026-09-07: the files land without the adaptations the selection names, so
what remains is written here rather than assumed done.

**With this the selection is complete.** All twenty working commands are installed — the parent's
backlog is empty, and `/discover` and `/mechanism` sit beside them on their own authority, 22 skills
for 20 selected commands. Nothing in the tree said so before this ticket, because nothing derives
*selected but not installed*; that missing derivation is
[01-0010.0100](./01-0010.0100-remaining-named-corpus.md)'s, recorded there when its roster was
deleted.

**Repaired on landing, under repair-and-report.** `/review-architecture` linked `../to-tickets/` and
`../plan-impl/`, both renamed here; the selection's *update calls to the current workflow* was
already owed, so the two links were repaired to `/ticket` and `/plan`. `/recall` arrived with a
`<project-local>` block naming three `docs/` files; the 2026-09-06 decision makes harness layout
shared rather than local, and the body already sends recall to those records, so the block went
without a replacement. `/setup-devops` was taken from M, not from H's mismatched `setup-project`,
as the selection directs.

## What to build

- Each of the five carries the adaptation its selection row names, applied rather than assumed:
  `/conclude` routes archive ownership to `/maintain`; `/dream`'s daily scheduling trigger is
  reviewed as a source policy and not turned into a schedule; `/recall` keeps F's priority for
  landed-but-unchecked work; `/review-architecture` calls the current workflow; `/setup-devops`
  keeps M's metadata.
- `/setup-devops` stops carrying another project's baggage. It writes decisions into `docs/cicd.md`,
  a document this project does not have and whose appearance in `/commit`'s local block was deleted
  on 2026-09-07 for being an import rather than an answer. Whether a core skill may name its own
  output document, and whether Sentry, MIT and *look up the best option on web* are global practice
  or one estate's opinion, is this ticket's to settle — not silently kept because the file arrived.
- Each of the five used once on real work here, which is what shows the body survives contact.

## Open issues

- Whether `/setup-devops` earns its place at all. It is the thinnest of the twenty and the most
  opinionated, and this project will never run it — its reader is a recipient, which is an argument
  for keeping it and against ever verifying it here.
- Whether `/review-architecture` overlaps [`/impact`](../../.agents/skills/impact/SKILL.md) and
  [`/tdd`](../../.agents/skills/tdd/SKILL.md) enough to be one of them. It leans on `tdd/deep-modules.md`
  for its central concept and ends by minting a ticket and an RFC, so its seams are already someone
  else's; [01-0016](./01-0016-responsibility-coherent.md) settles ownership, this ticket only asks
  whether the skill survives it.
- Whether `/recall` is the pacer's instruction file rather than a mechanism of its own. Raised on
  [01-0020](./01-0020-pacer.md), which owns session resumption and whose align has not run; deciding
  it here would split one mechanism across two tickets.

## Acceptance criteria

- [x] The five are installed under `.agents/skills/`, and a fresh session lists them. **2026-09-09:**
  `/recall`, `/review-architecture` and `/setup-devops` landed and were listed by the host in the
  same session; `/conclude` and `/dream` were already present.
- [ ] Each of the five carries its named selection adaptation, applied and visible in the file.
- [ ] Each has been used once on real work in this repository.
- [ ] `docs/cicd.md` has a disposition: written by `/setup-devops` as a core-declared output, or
  dropped from its body.
- [ ] No skill among the five references a path that does not resolve, except one it creates itself.
- [ ] `/verify` has been run on this ticket against its ticket, RFC, and governing docs.

## Out of scope

`/edge` and the other four of [01-0010.0100](./01-0010.0100-remaining-named-corpus.md)'s five.
Declaring any of these as a mechanism, and the allowlist rows for the ones that are not —
[01-0017](./01-0017-io-graph-coherent.md)'s, which owns every installed skill being named or
allowlisted. What each skill *owns* is [01-0016](./01-0016-responsibility-coherent.md)'s. The pacer's
relationship to `/recall` is [01-0020](./01-0020-pacer.md)'s.

## Parent scope addressed

The last five of the twenty selected working commands, and with them the parent's install backlog.
The parent keeps cross-project distribution, the owed core changes it lists, and the local-override
question.
