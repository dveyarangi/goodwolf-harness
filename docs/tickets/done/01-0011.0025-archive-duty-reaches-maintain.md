# The archive duty reaches the skill that archives

- **Status:** Done (2026-09-09)
- **Type:** HITL
- **Plan:** [implementation plan](../../rfc/done/01-0011.0025-archive-duty-reaches-maintain.md) — the
  shape amended first, the maintainer check-first, then the rules file, then the declaration
- **Depends on:** [01-0011.0020](./01-0011.0020-rules-one-home.md) (the injector that installs the
  rules), [01-0011.0022](./01-0011.0022-shape-survives-second-mechanism.md) (the shape, tested on a
  second mechanism)
- **Blocks:** [01-0011.0030](../01-0011.0030-archive-backlog-listed.md),
  [01-0011.0040](../01-0011.0040-queue-derived-index.md) — both are readings of `/ticket`'s records,
  and neither has a declared record shape to read until this lands;
  [01-0011.0050](01-0011.0050-shape-checked.md) — the third declaration it sweeps
- **Outcome:** `/ticket` is declared through the shape, paired close's rules live in one file and
  are installed into `/maintain` so it carries the archive duty rather than only the permission
  form, and `/ticket`'s records declare a shape that a script enforces.

## Parent

[A mechanism has one home for its rules, and a derived index](../../spec/01-0011-mechanism-shape.md).
`/impact`: [the 2026-09-07 re-slice assessment](../../spec/01-0011-mechanism-shape.md#impact--2026-09-07).

## Why this exists

This is the slice that closes the defect the whole spec was written from. On 2026-09-06 five
tickets sat finished and unarchived. The duty that requires the move is written in `/ticket`'s
files; `/maintain` owns archiving and carries only the permission form — *a ticket moves only when
every acceptance box is checked*. Three `/maintain` passes each correctly declined to touch records
they had not examined, and nothing swept, so the backlog was invisible rather than ignored.

Writing the duty into `/maintain` by hand would fix this instance and leave the next to chance.
Installing it from `/ticket`'s rules file is what makes the fix mechanical in both directions.

**Paired close is a behavior of `/ticket`, not a mechanism.** It is not a skill, and under one
instruction file per mechanism it cannot be one. `/maintain` is not a part of it: it is the target
the rule is installed into, and it is the mechanism for maintaining mechanisms.

## What to build

Every decision here is the user's, 2026-09-08, at this ticket's align and its `/plan`; the
[RFC](../../rfc/done/01-0011.0025-archive-duty-reaches-maintain.md) carries the shapes.

- **`/ticket` declared** at `.agents/mechanisms/ticket/`: its parts — the skill, the format shelf,
  the mover with its five test files, the maintainer with its tests; what it relies on — the
  corpus reader, the test harness and the installer, all `mechanism-shape`'s, since the always-on
  mechanism is the one that cannot leave and the spec's ownership decision names it where a
  shared part's claim bites; twenty-one moments, of which five are `not yet` on tickets that
  exist, two `elsewhere`, and one `unowned by design`; who decided each rule; what it produces
  and who reads it; **nothing retires it**, tracking work being core to the method.
- **`ticket.rules.md`**, six rules in three targets: P1 finished is every box checked, P2 close
  the pair in one invocation, P3 the header is the maintainer's, P5 run `tickets.py --check`,
  into `/maintain`; P6 name the RFC by the ticket's basename, into `/plan`; P7 land a decision in
  its durable home and rewrite the ticket in place, into `/align`. Each target's restatement
  becomes its installed block. **P4** — a spec's agreements before archiving it — is `/spec`'s and
  stays in `/maintain`'s body inside a straw dog bound to
  [01-0017](../01-0017-io-graph-coherent.md), which declares `/spec`; it is not authored here.
- **The ticket declared as a record** in `TICKET-FORMAT.md`: one file, tier 2, removed by paired
  close; the stage read from the `Plan` bullet — incepted hosts chunks, shaped keeps what is
  actual; the header one form, `Type` required, `Kind` gone, `Plan` exactly when the RFC exists,
  `Outcome` one sentence; the six named sections and at most one narrative when shaped; one
  acceptance box naming `/verify`; RFC pairing by name. Of an RFC, only pairing; its contents
  are `/plan`'s.
- **`tickets.py --check`**, check-only, live rows only, format never content: file, line and
  rule per diagnostic, a skip for a record it cannot read, exit status the verdict, nothing
  written. In the verification set.
- **Two amendments to the shape**, forced by the third declaration and recorded in the shape's
  evidence: `declared by` is gone *(the user, 2026-09-09, at `/implement`)* — it fed one check that
  never fired and duplicated the evidence's first paragraph, and a `not yet` row may not name an
  archived ticket instead, which needs no field; and the shape's doc carries *installing a
  declared mechanism into a tree*, `not yet` on the parent, where each mechanism's own row pointed
  until now.
- **Two bare-form headers repaired** to the bullet form, and `.0030`'s *both header forms*
  criterion struck.
- **The skill and shelf shed what this slice makes a second home**: *Completing a ticket* and
  *Layout* become pointers; the skeleton and the two tables become the record declaration; the
  strike convention leaves with P7; the queue section sits in a straw dog bound to
  [.0040](../01-0011.0040-queue-derived-index.md). Skill text that is merely long — the
  vertical-slice duplication, the optional explore step, the concern-index references, *PRD* —
  waits for [01-0016](../01-0016-responsibility-coherent.md) and 01-0017.
- **`docs/process.md`** names the maintainer in the verification set and points at the shelf for
  the RFC's basename instead of restating it. The evidence is at
  `docs/mechanisms/ticket.evidence.md`; no link from `.agents/skills/ticket/` into `docs/` sits
  outside a `<project-local>` block.

## Acceptance criteria

- [x] `.agents/mechanisms/ticket/` holds a doc naming every part with its standing, listing
  `docs_corpus.py` and `tests/harness.py` as relied-on with `mechanism-shape` as owner; the shape's
  doc lists both as parts and `/maintain`'s doc names the same owner, so no doc still declares
  the gap `01-0011.0022` declared.
- [x] The moments table covers every moment; every `not yet` names a ticket that exists.
- [x] The shape's doc carries the install-into-a-tree moment; no doc and no shelf carries a
  `declared by` bullet; the check reports a `not yet` row naming an archived ticket, and a test
  proves a ticket a close moves into `done/` is reported after the move.
- [x] `ticket.rules.md` holds P1, P2, P3, P5, P6 and P7, each section naming its targets and
  authority, and the anchor table names `/maintain`, `/plan` and `/align`. P4 is not in it.
- [x] Installing writes one owned, delimited block into each of `/maintain`, `/plan` and `/align`;
  `/maintain` then states the archive duty and the maintainer to run, `/plan` the basename rule,
  `/align` the resolution rule, and none of the three restates its block in its own words.
- [x] Removing leaves each target byte-identical to its pre-installation bytes.
- [x] `/maintain`'s hand copy of P1–P4 is retired; P4 stands alone in a straw dog bound to
  `01-0017`; `/maintain`'s doc has no `embedded` row left and its straw dog for this ticket is gone.
- [x] `TICKET-FORMAT.md` declares the record: one ticket file, tier 2, the header fields with
  their presence and order, `Type` required, `Kind` gone, `Plan` present exactly when the RFC
  exists, `Outcome` one sentence, the sections each stage admits, the `/verify` box, RFC pairing,
  and paired close as what removes a finished entry.
- [x] `tickets.py --check` reports a live record violating that shape with file, line and rule,
  reports a half-closed pair, reports an unparseable record rather than skipping it, writes
  nothing, and reads nothing under `done/`.
- [x] The two bare-form headers are repaired, the shape admits the bullet form only, and
  `.0030`'s *both header forms* criterion is struck.
- [x] `/ticket`'s skill and shelf carry no sentence a rule this ticket installs or declares makes
  a second home: *Completing a ticket* and *Layout* are pointers, the skeleton and the two tables
  are one declaration, the strike convention is gone, and the queue section sits in a straw dog
  bound to `.0040`.
- [x] `docs/process.md` no longer states a rule paired close owns, and states the ones it does not.
- [x] No link from `.agents/skills/ticket/` into `docs/` sits outside a `<project-local>` block.
- [x] `.agents/README.md`'s *Mechanical support* paragraph names `tickets.py`.
- [x] Behavioral tests cover the maintainer over conforming, violating, unparseable and archived
  records and over a half-closed pair, and the installer over the two new targets.
- [x] The grade `/maintain`'s doc pre-registered for this session — did its declaration receive
  the installed block and the ownership repair as written, or did the doc have to be rewritten —
  is answered in `/maintain`'s evidence.
- [x] `/verify` has been run on this ticket against its ticket, RFC, and governing docs, and the
  ticket held its shaped form (2026-09-09).

## Out of scope

The archive backlog listing ([0030](../01-0011.0030-archive-backlog-listed.md)), which asks which
records are *finished and unmoved* — a question about content, not format, and not the maintainer's.
The queue index ([0040](../01-0011.0040-queue-derived-index.md)). `move_doc.py`'s behavior: its
**rules** move homes here, the mover does not change. The remainder of `docs/process.md`, owned by
[01-0020](../01-0020-pacer.md) and [01-0016](../01-0016-responsibility-coherent.md). Declaring any
fourth mechanism — [01-0017](../01-0017-io-graph-coherent.md) applies the shape to the rest.

## Parent scope addressed

Stories 1, 2, 7, 14, 21, 22.
