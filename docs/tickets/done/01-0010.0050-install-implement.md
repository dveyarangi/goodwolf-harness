# Install the implementation mechanism

- **Status:** Done (2026-09-06)
- **Type:** HITL
- **Plan:** [Install /implement — implementation plan](../../rfc/done/01-0010.0050-install-implement.md) — implement, tdd, improve-comments; first use is this slice
- **Depends on:** [Install /plan](./01-0010.0040-install-plan.md) (an RFC from `/plan`'s first use)
- **Outcome:** The implementation mechanism is installed — `/implement`, `/tdd` with its supporting files, and `/improve-comments` — discoverable, and used once to implement a planned harness slice.

## Parent

[Shared harness across projects](../01-0010-dev-harness-shared-and-local.md). Loop-completion slice from [the spec](../../spec/01-0010-dev-harness-shared-and-local.md#harness-loop-on-itself--agreed-2026-09-06). `/impact`: [Delivery-ring breakdown](../../spec/01-0010-dev-harness-shared-and-local.md#delivery-ring-breakdown--2026-09-06).

## What to build

- The implementation mechanism, not the `/implement` skill alone: Meteoscape `implement` as `/implement`, Meteoscape `tdd` as `/tdd` with its five supporting files, and Meteoscape `improve-comments` as `/improve-comments`.
- Handoff from `/implement` uses `/verify` and `/maintain`, not `/review-impl` or `/sync-arch`. This project's validation tools and repair policy; `commit=ask`.
- First use: implement this slice from its RFC. `/implement` is planned with `/plan`.

## Decisions this ticket's align owns

- ~~What 0050 installs: the `/implement` skill, or the implementation mechanism.~~ **2026-09-06, user: the mechanism, including its dependencies.** Membership is `/implement` + `/tdd` (and its files) + `/improve-comments`. `/verify` and `/maintain` stay later ring mechanisms. `/commit` stays the autonomy switch.
- ~~Where `/implement` keeps this project's checks.~~ **2026-09-06, user:** generalize the typecheck/suite line; the verification set is linked from [process](../../process.md#verification), not inlined. No `<project-local>` on `/implement` for that. `/verify` is the verification of landed work.

## Acceptance criteria

- [x] `/implement` is under `.agents/skills/implement/`, and a fresh session lists it the same way `/spec` is listed. **2026-09-06, Cursor:** `.claude/skills/implement/SKILL.md` and `.cursor/skills/implement/SKILL.md` resolve through the existing links, the same loader path that listed `/spec`.
- [x] `/tdd` is under `.agents/skills/tdd/` with its five supporting files, and a fresh session lists it. **2026-09-06, Cursor:** `.claude/skills/tdd/SKILL.md` and `.cursor/skills/tdd/SKILL.md` resolve; the five files are hash-identical to Meteoscape.
- [x] `/improve-comments` is under `.agents/skills/improve-comments/`, and a fresh session lists it. **2026-09-06, Cursor:** `.claude/skills/improve-comments/SKILL.md` and `.cursor/skills/improve-comments/SKILL.md` resolve; body hash-identical to Meteoscape.
- [x] `/implement` is used once to implement a planned harness slice. **2026-09-06:** this install, following `/implement` after the bodies were on disk.
- [x] Installed files name `/verify` and `/maintain` at the handoff; they do not name `/review-impl` or `/sync-arch`. `/implement` names `/tdd` and `/improve-comments`. **2026-09-06:** `/review-impl` is absent from `.agents`. `/sync-arch` remains only in `align/EDGE-FORMAT.md` (pre-existing `/edge` shelf; RFC leave-alone `/edge`; this slice's folders are clean).
- [x] `AGENTS.md`'s local block lists `/implement`, `/tdd` and `/improve-comments`. **2026-09-06:** `Installed: /align, /impact, /ticket, /spec, /plan, /implement, /tdd, /improve-comments, /verify`.
- [x] `/verify` has been run on this ticket against its ticket, RFC, and governing docs. **2026-09-06:** this install. Match: three folders, first use on this slice, `name:` frontmatter, `/plan` on the tdd planning gate, verification set linked not inlined, no `<project-local>` on `/implement`, RFC not moved, `/maintain` not installed. Verification set empty. This ticket is not moved to `done/` in this pass. **Repair:** queue sentence "No implementation spec or RFC is approved yet" contradicted the queue's job as current state; removed. Remaining: TICKET-FORMAT still says paired `done/` close — 0070; [01-0010.0040](./01-0010.0040-install-plan.md) still In progress until its `/verify`.

## Out of scope

Installing `/verify` or `/maintain`. Those are later slices. `/verify` names `/improve-comments`; that cite is 0060's, not a reinstall here.

## Parent scope addressed

Story 11; selection rows `/implement`, `/tdd`, `/improve-comments`.
