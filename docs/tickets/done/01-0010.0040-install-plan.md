# Install /plan

- **Status:** Done (2026-09-06)
- **Type:** HITL
- **Plan:** [Install /plan — implementation plan](../../rfc/done/01-0010.0040-install-plan.md) — install `/plan`, first use writes 0050's RFC
- **Depends on:** [Install /spec](../01-0010.0035-install-spec.md) (the spec and `/spec`)
- **Outcome:** `/plan` is installed from the accepted selection, discoverable, and used once to write an RFC for the next ring slice, calling `/impact` as the entry file says.

## Parent

[Shared harness across projects](../01-0010-dev-harness-shared-and-local.md). Loop-completion slice from [the spec](../../spec/01-0010-dev-harness-shared-and-local.md#harness-loop-on-itself--agreed-2026-09-06). `/impact`: [Delivery-ring breakdown](../../spec/01-0010-dev-harness-shared-and-local.md#delivery-ring-breakdown--2026-09-06).

## What to build

- Forecast Collector `plan-impl` installed as `/plan`. Do not import its `Pass` line or Cursor loop configuration. Callers use `/plan` and `/ticket`. `/plan` calls `/impact` where the entry file says it does.
- First use: write the RFC for [01-0010.0050](../01-0010.0050-install-implement.md). `/plan` is planned by reading `/plan`.

## Acceptance criteria

- [x] `/plan` is under `.agents/skills/plan/`, and a fresh session lists it the same way `/spec` is listed. **2026-09-06, Cursor:** `.claude/skills/plan/SKILL.md` and `.cursor/skills/plan/SKILL.md` resolve through the existing links, the same loader path that listed `/spec`.
- [x] `/plan` is used once to write an RFC for the next ring slice, with `/impact` on that plan. **2026-09-06:** [01-0010.0050](../../rfc/01-0010.0050-install-implement.md) has an Impact section.
- [x] Installed files name `/plan` and `/ticket`; they do not name `plan-impl`. **2026-09-06:** grep of `.agents` is clean; `name: plan`; callers `/plan` and `/ticket`; no `Pass` line.
- [x] `AGENTS.md`'s local block lists `/plan` among the installed skills. **2026-09-06:** `Installed:` includes `/plan`.
- [x] `/verify` has been run on this ticket against its ticket, RFC, and governing docs. **2026-09-06:** this install. Match: `name: plan`, `/impact` recorded on first-use RFC, no `plan-impl`/`Pass` in the skill. This ticket is not moved to `done/` in this pass. **Repair:** RFC TICKET-FORMAT link from `docs/rfc/` was `../.agents/...` (dead); now `../../.agents/...`. RFC `<project-local>` shape contradicted the accepted rule that `/plan` has no project-specific fact; dropped. Remaining: TICKET-FORMAT still says paired `done/` close — 0070.

## Out of scope

Installing `/implement`, `/verify`, `/maintain`. Those are later slices.

## Parent scope addressed

Story 11; selection `/plan`; core change that `/plan` calls `/impact`.
