# Delivery status

**Last updated:** 2026-09-23

**Completed step:** `/ticket` on [01-0010.0160](01-0010.0160-harness-edge-changes-reach-an-update.md), 2026-09-23 — the releases work is decomposed into three slices, `.0185` → `.0190` → `.0195`, behind [01-0010.0155](01-0010.0155-a-tree-names-the-repository-its-core-comes-from.md). `/impact`: proceed, narrowed to three. The fourth drafted slice — a tree holding a ref older than the first release — folded into `.0195`, because an old recipient's announce line carries its own repository and `--check` against it keeps working: only the pending-release count breaks, so the migration is one note and one re-stamped `--update --from`. Two hidden edges went to the slices that own them: whether core may name the edge record at all, which makes `.0185` decision-bearing, and the publish's history question, which is `.0190`'s. `.0160` is now the end state and takes no RFC of its own. Earlier the same day: `/align` and `/ticket` on **arrival-to-ready** — nine decisions landed on [01-0010](01-0010-dev-harness-shared-and-local.md#arrival-to-ready--2026-09-23) and four slices minted, `.0165` → `.0170` → `.0175` → `.0180`. The customer is ai-game-1, installed at `goodwolf-harness@47516ce`: its verification set came out holding core's own gates and nothing of the project's, its painted doors were never created, and the install's last step ran before the project had a toolchain to read. Decided: the sequence (install+gate → L1/L2 → setup → quicklook → L3 → inject → check); doors created by the install with `docs/tickets/README.md` carrying an arrival state; one fixed quicklook whose admission scales rather than its effort, with *unread* distinct from *deferred*; harness owning every moment, `setup-devops` relied-on; the local file's extraction split so the switches precede the step they authorise; `git init` a refusal bullet and a remote offered, never created; L3 a pointer to `docs/cicd.md` with core's gates additive only. Two rule failures registered and both amendments landed the same day: [7](../rule-failures.md) — harness's declaration carried its script's verbs, repaired in `MECHANISM-FORMAT` and in the declaration — and [8](../rule-failures.md) — `/impact`'s *Output only* ended its caller's turn, repaired in `/impact`. Before it: `/align` on [01-0010.0160](01-0010.0160-harness-edge-changes-reach-an-update.md), 2026-09-21 — four decisions landed: what the installation edge is, its two homes (the engineering record at the origin, the manifested edge derived from it), registration at the occasion and derivation at the release, and *releases* as the mechanism that publishes the clean core to a repository of its own. The update defaults to the latest release and the installing agent decides whether to take it. Before it the same day: `/plan` and `/implement` and `/verify` on [01-0010.0145](01-0010.0145-core-scripts-under-one-directory.md), the scripts' move, whose own breakage is the evidence the align read. Earlier: `/maintain` on [01-0010.0130](done/01-0010.0130-harness-installs-into-another-tree.md) — the paired close of the slice that made the harness install itself: the user graded it in frost_map, whose own `harness.py . --check` reported `arrived: true` at `goodwolf-harness@2c733f0` with both links resolving and its four local rules re-injected. Before the close the same day: `/implement` landed the `harness` mechanism, its `/verify`, and a first live run that refuted four things (the shipped suite in the gate, the links as parts, one `cat-file` per file, an archive that smudged), all on the mechanism's evidence. What every earlier pass decided is on the ticket that owns it, in the mechanism evidence under `docs/mechanisms/`, and in the session records under `docs/sessions/`.

**Candidate:** [01-0010.0155](01-0010.0155-a-tree-names-the-repository-its-core-comes-from.md) (AFK, unblocked since `.0145` landed), then [01-0010.0185](01-0010.0185-an-edge-change-writes-its-line.md)'s own align. That is the releases chain in order: the repository line first, so `.0195` writes the core repository's URL once in its final home; then the registration, whose align owns whether core may name the edge record at all; then [01-0010.0190](01-0010.0190-core-is-published-as-a-release.md) and [01-0010.0195](01-0010.0195-a-recipient-reads-before-it-takes.md). The chain ranks first because two real trees are stranded behind it right now — frost_map at `2c733f0` and ai-game-1 at `47516ce`, both holding verification sets that name `.agents/scripts/`, which [01-0010.0145](01-0010.0145-core-scripts-under-one-directory.md) moved, neither told and neither able to ask. `.0145`'s last box is frost_map's update, which is `.0195`'s acceptance: hold it, or take it blind and record what the report did not say. Then [01-0010.0150](01-0010.0150-harness-meets-a-tree-with-a-method.md), whose RFC is authored and whose dependency has landed, and [01-0010.0155](01-0010.0155-a-tree-names-the-repository-its-core-comes-from.md) (the repository line, AFK), which points at the published core once releases exists. Beside them the arrival-to-ready family, minted 2026-09-23: [01-0010.0165](01-0010.0165-doors-arrive-with-the-tree.md) is the cheapest independent start — AFK, no dependency, and it makes the glossary's *every recipient has a painted door* true instead of false — then [01-0010.0170](01-0010.0170-arrival-becomes-a-sequence.md), which is the slice that actually repairs what ai-game-1 showed. By the ordering rule below they rank high: `.0165` hands `.0170` the doors, and `.0170` hands every later install a sequence that does not fall back to verifying core. Behind them, by the chain, [01-0012](01-0012-hierarchy-coherent.md)'s `/plan` opens the four coherence refactors; whether the pacer still waits behind them is [01-0020](01-0020-pacer.md)'s open question, which now also owns making the loop's ceremony switchable. **Next cycle needs a nod** (`next-cycle=ask`). Resume by opening a fresh session here: its first line is the entry contract [AGENTS.md](../../AGENTS.md) declares and its first act is `/recall`; what follows the wake until the pacer exists is [pacer.md](../pacer.md#core-rules--under-alignment)'s.

**The chain:** [01-0010.0130](done/01-0010.0130-harness-installs-into-another-tree.md), the install half of [the install spec](../spec/01-0010.0130-harness-installs-into-another-tree.md), closed 2026-09-21; the spec's install half is delivered and its parked items — contribution back, a workspace-level harness — stay on the spec. Separation precedes the coherence chain *(the user, 2026-09-09, on [the frost_map probe](../research/separation-probe-frost-map.md))*; behind it the spec's ordering is unchanged: the four rule refactors, then [01-0020](01-0020-pacer.md), which wants the Tier 1 placement rule that [01-0018](01-0018-reachability-coherent.md) settles; [01-0010.0080](01-0010.0080-impact-work-shape.md) stays Ready beside them. [01-0020](01-0020-pacer.md)'s align is open on the wake rule alone, landed 2026-09-20; its remaining decisions stay open.

**Two decisions wait at `/align`**, held here because nothing else holds them: whether ADRs or tickets are the home for a decision, which the entry file currently answers both ways; and what an entry-contract version covers, which today names five of its nine sections.

<straw-dog until="01-0020 is done" ticket="docs/tickets/01-0020-pacer.md">
**How this queue is ordered** *(the user, 2026-09-07)*: the harness is self-building, so the governing consideration is which slice hands the next one a tool that makes the harness work better — not dependency order alone, and not size. A slice that only describes something ranks below one that gives the next slice a script, a check or a rule it can lean on. The pacer owns this once it exists ([01-0020](01-0020-pacer.md)); until then it is stated here and applied by hand.
</straw-dog>
Open idea, unowned: `/impact` could name the *error classes that stay uncaught* if a slice is not built, and the queue could rank by which noise is loudest — a candidate extension of [01-0010.0080](01-0010.0080-impact-work-shape.md), not yet its scope.

**Working documents:** [Harness spec — draft](../spec/01-0010-dev-harness-shared-and-local.md), [the install spec — accepted 2026-09-14](../spec/01-0010.0130-harness-installs-into-another-tree.md), [working glossary](../glossary.md), and [pacer.md](../pacer.md), which holds the pacer's core rules resolved so far until [01-0020](01-0020-pacer.md) carries them. `docs/process.md` is preliminary under a `<straw-dog>` block; the pacer owns it once it exists.

**The verification set** is `/verify`'s local block, installed from `local.rules.md`: the behavioral suite, the mechanism check — declarations, skill claims and every path under `docs/` core names — the installer's check and the ticket maintainer; 311 tests at the close of 2026-09-21, one skipped where the platform makes no symlink. The straw dogs are what `straw_dogs.py docs AGENTS.md local.rules.md .agents` lists: 66 at that close, all bound, no condition met.

| Ticket | Status | Type | Outcome |
|---|---|---|---|
| [Shared harness across projects](01-0010-dev-harness-shared-and-local.md) | In progress | HITL | Projects share a canonical development method, contribute improvements to it, and receive accepted changes mechanically while preserving project-specific behavior across Claude Code, Codex and Cursor. |
| [/impact recommends the work's shape](01-0010.0080-impact-work-shape.md) | Ready | HITL | `/impact` says whether the work it assessed is a spec, a ticket, an RFC or a re-slice, alongside its blast-radius verdict, so `/ticket` and `/plan` receive a routing answer instead of inferring one. |
| [The backlog five arrive with their adaptations](01-0010.0105-backlog-five-arrive.md) | Partial (three installed 2026-09-09) | HITL | `/conclude`, `/recall`, `/dream`, `/review-architecture` and `/setup-devops` carry the adaptations the selection names for them, each has been used once here, and the twenty selected working commands are complete with nothing left in the parent's backlog. |
| [Finished work that is not archived is listed](01-0011.0030-archive-backlog-listed.md) | Ready | AFK | A command lists records whose work is finished and whose folder does not say so, and the standing count is held as a ratchet — silent at or below its mark, loud when it rises. |
| [The queue is read from the tickets](01-0011.0040-queue-derived-index.md) | Ready | HITL | The queue's status, type and outcome are derived from the ticket headers that own them, so the queue cannot contradict a ticket, and the pacing prose stays hand-written. |
| [A mechanism is re-checked when what governs it moves](01-0011.0060-mechanism-rechecked-when-governing-moves.md) | Planned (own align precedes; 01-0010.0120 open) | HITL | `/maintain` reads which mechanisms are due from a derivation over the tree rather than inferring it: a mechanism's doc is due when the meta-rules moved since it was last checked, its records are due when its own surfaces moved or its records churned, the last check is a mark that only the closing step of a maintenance can move, and dueness is derived at every look and never stored. |
| [Hierarchy reads the same everywhere](01-0012-hierarchy-coherent.md) | Ready | HITL | Hierarchy — from what height a thing is looked at — has one account, and every statement about height across the entry file, process, glossary and skills either is that account or points at it. |
| [Scope reads the same everywhere](01-0014-scope-coherent.md) | Planned (hierarchy precedes) | HITL | Scope — the extent of what a pass, a ticket or a document covers — has one account, and every scope statement across the harness either is that account or points at it. |
| [Each skill owns its own responsibility](01-0016-responsibility-coherent.md) | Planned (hierarchy and scope precede) | HITL | Every skill states only what it owns and links for the rest; a rule appears once, in the skill responsible for it; contradictions between skills are resolved rather than coexisting. |
| [Every skill and document has named producers and consumers](01-0017-io-graph-coherent.md) | Planned (the first mechanism precedes) | HITL | Every mechanism in this tree is declared to the shape paired close proved, every installed skill is named by one or allowlisted with its reason, and the documents four skills transact against either exist with an owner or stop being referenced. |
| [A term reaches a record only if the glossary defines it](01-0017.0010-terms-defined-before-they-land.md) | In progress (align underway) | HITL | A term used as shared vocabulary is either defined in the glossary or is stopped as a decision before it reaches a record, every glossary rule has one authored home, and the rule that enforces this is where the occasion is rather than inside one skill nobody had invoked. |
| [Changing how the project builds is one edit](01-0017.0020-practice-swaps-in-one-edit.md) | In progress (align underway) | HITL | The transferable practice this harness carries — how tests are written, how architecture is documented — has one owner, so adopting a different practice is an edit to one mechanism instead of a refactor across scattered skills, and what belongs to that corpus rather than to the harness's own loop is settled rather than assumed. |
| [Rules reach the occasion they are for](01-0018-reachability-coherent.md) | Planned (responsibility precedes) | HITL | A rule or skill that applies at an occasion is actually in front of the agent at that occasion, and where it cannot be, the gap is named rather than assumed away. |
| [Core's scripts cannot collide with a project's own](01-0010.0145-core-scripts-under-one-directory.md) | Partial (landed and verified 2026-09-21; frost_map's grade remains) | AFK | Every script core ships sits under one directory of its own beneath `.agents/scripts/`, tests included, so an install into a tree that already has scripts there writes beside them and never over them, and every live citation of a script names where it is. |
| [The harness meets a tree that already has a method](01-0010.0150-harness-meets-a-tree-with-a-method.md) | Planned (RFC authored 2026-09-21; the scripts' namespace precedes) | HITL | An install into a tree that already has a method — this harness taken by hand, or a stranger's entry file, stub, skills and process — ends with that method's content carried into the local file or a claimed skill, aligned with the user where it collides with core, or left where it is, never lost; and the skill says each step. |
| [A tree names the repository its core comes from](01-0010.0155-a-tree-names-the-repository-its-core-comes-from.md) | Planned (the scripts' namespace precedes) | AFK | An agent reading the harness skill in any tree sees which repository its core comes from, a recipient runs `--update` and `--check` with no `--from`, and a fork edits one line and everything it installs names the fork. |
| [A change to the harness's edge reaches a recipient as a release it can read before taking it](01-0010.0160-harness-edge-changes-reach-an-update.md) | In progress (decomposed 2026-09-23 into `.0185` → `.0190` → `.0195`; end state, no RFC of its own) | HITL | Core is published as releases to a repository of its own, each carrying a note written for an instance — what moved, from where to where, what the instance amends — and a recipient reads the notes standing between its ref and the latest before it decides whether to take them. |
| [An edge change writes its line as it is made](01-0010.0185-an-edge-change-writes-its-line.md) | Planned (decision-bearing; its own align precedes the feature) | HITL | A change to the installation edge has its line in the edge record in the pass that made it, held there by a rule that fires at the occasion rather than by whoever remembered. |
| [Core is published as a release to a repository of its own](01-0010.0190-core-is-published-as-a-release.md) | Planned | HITL | Core is published to a repository of its own, each publish a release carrying a note written for an instance, so a recipient installs from what was published rather than from this working tree. |
| [A recipient reads what stands between it and the latest before it takes it](01-0010.0195-a-recipient-reads-before-it-takes.md) | Planned | HITL | A recipient runs `--check` and learns which releases stand between its announced ref and the latest, reads what each asks of it, and decides — instead of taking a change blind or never learning one exists. |
| [A fresh tree arrives with its painted doors](01-0010.0165-doors-arrive-with-the-tree.md) | Planned | AFK | An install into a fresh tree ends with every painted door present and `docs/tickets/README.md` readable, so the first session's `/recall` has a delivery status to stand on instead of nothing. |
| [The first install's last step becomes a sequence](01-0010.0170-arrival-becomes-a-sequence.md) | Planned | AFK | A first install ends with a local file whose verification set names the project's own checks or states that it has none, instead of falling back to core's gates. |
| [A tree's existing material becomes its glossary and a skeleton](01-0010.0175-arrival-describes-what-it-finds.md) | Planned (narrowed at the 2026-09-23 `/impact`; the codebase half is not in scope) | HITL | An install into a tree that already holds written material ends with a glossary and an architecture skeleton drawn from it, and every section nobody read stamped so, rather than a tree that arrives undescribed. |
| [Harness names what `/maintain` must check for it](01-0010.0180-harness-names-its-maintenance.md) | Planned | AFK | A `/maintain` pass over a tree that holds core re-checks what the harness owns there, instead of harness being the one mechanism that asks for nothing. |
| [What each host can say without being asked](01-0010.0120-host-delivery-surfaces.md) | Ready | HITL | The harness knows, from observation rather than assumption, what Claude Code, Codex and Cursor each place in front of an agent without being asked; that capability is recorded with what cannot be confirmed about it; and the decision of whether this harness takes a dynamic tier-1 surface at all is landed in its durable home, unblocking the slices that assume one. |
| [The remaining named corpus arrives](01-0010.0100-remaining-named-corpus.md) | Partial (all five installed by 2026-09-09; nothing else met) | HITL | `/advise`, `/skill-up`, `/commit`, `/celebrate` and `/edge` are installed from the accepted selection, discoverable, each used once, and the core changes they owe are landed with them. |
| [The harness changes its own rules by stated meta-rules](01-0019-harness-amends-itself-by-explicit-meta-rules.md) | Blocked (accumulating evidence) | HITL | Amending the harness's own rules follows stated meta-rules — how a failure is registered, when a rule is reworded, what counts as evidence that it did not work — instead of a judgement made afresh each time; and whether that is its own mechanism is answered from strikes rather than from argument. |
| [Pacer](01-0020-pacer.md) | In progress (align underway; first core rule landed 2026-09-20) | HITL | The agent knows, on every turn, what it does after `/recall` and what the turn's reply is scoped to, from Tier 1 rules in the entry file; and after a session break the next session resumes the delivery ring at the step it stopped, stopping only for a HITL escalation. |

[The adoption shortlist](01-0010-dev-harness-shared-and-local.md#adoption-shortlist--recommendations-awaiting-alignment) retains the broader open design decisions; [the initial comparison](../research/audit-2026-09-05/REPORT.md) and [Life findings](../research/life-harness-findings.md) supply the evidence behind them.
