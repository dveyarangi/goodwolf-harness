# Install /maintain

- **Status:** Ready
- **Type:** HITL
- **Depends on:** [Install /verify](./01-0010.0060-install-verify.md) (a verified slice to close)
- **Outcome:** `/maintain` is installed, discoverable, and used once to complete a paired ticket+RFC close; it enumerates `<temporary>` blocks; denoise and sync-arch composition is aligned on this ticket.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). Loop-completion slice from [the spec](../spec/01-0010-dev-harness-shared-and-local.md#harness-loop-on-itself--agreed-2026-09-06). `/impact`: [Delivery-ring breakdown](../spec/01-0010-dev-harness-shared-and-local.md#delivery-ring-breakdown--2026-09-06).

## What to build

- Compose `/maintain` from Meteoscape `denoise` and `sync-arch`, plus the owed `<temporary>` enumerator. Forecast Collector's newline-compatible mover is the starting point for paired close: ticket → `docs/tickets/done/`, RFC → `docs/rfc/done/`, citations repaired in one pass. TICKET-FORMAT already names this; the script does not exist yet.
- First use is a paired close of a just-finished slice, not a whole-tree sync-arch. Sync-arch composition is this ticket's align; it is heavy and must not widen the first use.

## Decisions this ticket's align owns

- How denoise and sync-arch compose into one `/maintain` without making the first close wait on a whole-tree architecture sync.
- What the mover's first live invocation covers versus later whole-tree passes.

## Acceptance criteria

- [ ] `/maintain` is under `.agents/skills/maintain/`, and a fresh session lists it the same way `/spec` is listed.
- [ ] `/maintain` is used once to complete a paired ticket+RFC close; both records are in their `done/` folders with citations repaired.
- [ ] `/maintain` enumerates `<temporary>` statements in scope and reports or removes the ones whose condition holds.
- [ ] `AGENTS.md`'s local block lists `/maintain` among the installed skills.
- [ ] `/verify` has been run on this ticket against its ticket, RFC, and governing docs.

## Out of scope

The pacer, `/edge`, remaining selected corpus. Whole-tree sync-arch as the first use.

## Parent scope addressed

Stories 7 and 11; selection `denoise`/`sync-arch`; paired close; `<temporary>` enumerator.
