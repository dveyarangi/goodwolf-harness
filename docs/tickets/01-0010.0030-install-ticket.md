# Install /ticket

- **Status:** Ready
- **Type:** HITL
- **Depends on:** [Align on harness work in Codex, Cursor and Claude Code](./01-0010.0020-live-alignment-across-hosts.md) for the Claude Code path only; its Codex and Cursor trials are not prerequisites.
- **Outcome:** `/ticket` is installed under `.agents/skills` from the accepted selection, discoverable in Claude Code, and used once here to mint a real slice, with `/impact` called from it as the entry file says.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). Second delivery slice; extends the proven Claude Code entry path to ticket decomposition. Minted 2026-09-06 to bind the `<temporary until="/ticket is installed">` block in `AGENTS.md` to real work.

## What to build

- The skill body from Meteoscape's `to-tickets` plus Forecast Collector's impact assessment of proposed slices, per the [selection table](./01-0010-dev-harness-shared-and-local.md#bootstrap-corpus-selection), installed as `/ticket`. Legacy `to-tickets` is not an alias.
- `TICKET-FORMAT.md` from Meteoscape, adapted to the naming already used here (`01-NNNN-slug`, four-digit child positions stepping by ten, `done/` on completion). Once installed it owns the ticket shape; `docs/process.md#naming` points at it.
- The core change owed by this install: `/ticket` calls `/impact` on the proposed slices and records the recommended shape.
- `align/EDGE-FORMAT.md`'s cite of `../to-tickets/TICKET-FORMAT.md` repaired to the installed path.
- The `<temporary until="/ticket is installed">` block in `AGENTS.md` removed, and its statement replaced by what is then true.

## Decisions this ticket's align owns

- Whether `/ticket` also owns the queue's table shape, or the queue stays hand-written until `/maintain` derives it. Recommendation at minting: `/ticket` registers its slices in the queue in the same pass, as the source skill does; derivation is `/maintain`'s later.
- Breakdown approval under `breakdown=ask`: what one approval covers when the breakdown is amended after impact.

## Acceptance criteria

- [ ] `/ticket` and its format file are under `.agents/skills/ticket/`, and a fresh Claude Code session lists it in its catalog.
- [ ] One real slice is minted through it, with an `/impact` assessment recorded, and the breakdown approved by the user before minting.
- [ ] `EDGE-FORMAT.md` resolves; no installed file cites `to-tickets`.
- [ ] The `AGENTS.md` temporary block is gone and the entry contract still announces v2, or the version is bumped with the reason recorded here.
