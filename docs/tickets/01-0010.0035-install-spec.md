# Install /spec

- **Status:** Done (2026-09-06)
- **Type:** HITL
- **Plan:** [Install /spec — implementation plan](../rfc/01-0010.0035-install-spec.md) — install `/spec`, develop the existing draft in place
- **Depends on:** [Install /ticket](./01-0010.0030-install-ticket.md) (this mint); [Align on harness work in Codex, Cursor and Claude Code](./01-0010.0020-live-alignment-across-hosts.md) (the Claude Code entry path)
- **Outcome:** `/spec` is installed from the accepted selection, discoverable, and used once to develop the spec for completing this harness's delivery ring; `/plan`, `/implement`, `/verify` and `/maintain` follow that spec.

## Parent

[Shared harness across projects](./01-0010-dev-harness-shared-and-local.md). Third delivery slice; spec-first, then the natural course. `/impact` on this split: [Second bootstrap delivery](./01-0010-dev-harness-shared-and-local.md#second-bootstrap-delivery--2026-09-06).

## What to build

- The skill body from Meteoscape/Forecast Collector `to-spec`, installed as `/spec`, per the [selection table](./01-0010-dev-harness-shared-and-local.md#bootstrap-corpus-selection). Callers use `/spec` and `/ticket`, not `to-spec`/`to-tickets`.
- First use: `/spec` develops the existing [harness spec draft](../spec/01-0010-dev-harness-shared-and-local.md) so it includes completing the delivery ring on this harness — `/plan` → `/implement` → `/verify` → `/maintain`, applied here, with finished tickets in `done/` only after `/verify`. `/plan` is planned by reading `/plan`.
- `/plan`, `/implement`, `/verify` and `/maintain` are not installed or minted here. They are sliced from that spec.

## Decisions this ticket's align owns

- ~~How `/spec` treats the existing draft: develop it in place, or write a new file.~~ **2026-09-06, user: in place.** The draft is already the owning spec for this parent.

## Acceptance criteria

- [x] `/spec` is under `.agents/skills/spec/`, and a fresh session lists it in the catalog the same way `/ticket` is listed. **2026-09-06, Cursor:** `.claude/skills/spec/SKILL.md` and `.cursor/skills/spec/SKILL.md` resolve through the existing links, the same loader path that listed `/ticket`.
- [x] `/spec` is used once to develop the spec for completing the delivery ring on this harness; loop completion is a requirement in that spec, not a separate mint. **2026-09-06:** story 11 and "Harness loop on itself" in [the draft](../spec/01-0010-dev-harness-shared-and-local.md).
- [x] Installed files name `/spec` and `/ticket`; they do not name `to-spec` or `to-tickets`. **2026-09-06:** grep of `.agents` is clean.
- [x] `AGENTS.md`'s local block lists `/spec` among the installed skills. **2026-09-06:** `Installed: /align, /impact, /ticket, /spec`.
- [x] `/verify` has been run on this ticket against its ticket, RFC, and governing docs. **2026-09-06:** first use of `/verify` (0060). Match: `name: spec` under `.agents/skills/spec/`, first use in place (story 11 and harness loop), no `to-spec`/`to-tickets` in `.agents`, `AGENTS.md` lists `/spec`. `.claude/skills/spec/SKILL.md` resolves. No repair.

## Out of scope

Installing `/plan`, `/implement`, `/verify`, `/maintain`, or the pacer. Those follow the spec.

## Parent scope addressed

Selection row `/spec`; spec-first; loop completion as spec content. Stories 5–10 of the draft remain the spec's, not this install's, to close.
