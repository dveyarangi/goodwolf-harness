# Install /spec — implementation plan

**Authored:** 2026-09-06

Implements [01-0010.0035](../../tickets/done/01-0010.0035-install-spec.md): install `/spec` from the accepted `to-spec` body and use it once on the existing harness spec.

## Scope

Install one skill folder, `.agents/skills/spec/`, the same recipient layout as `/ticket`. First use amends [the existing draft](../../spec/01-0010-dev-harness-shared-and-local.md) in place so that spec requires completing the delivery ring on this harness. `/plan`, `/implement`, `/verify`, `/maintain` are not installed.

## Shapes

- Folder name `spec`, frontmatter `name: spec`. Hosts discover it the same way they discover `ticket`.
- Body is Meteoscape/Forecast Collector `to-spec` with selected names: `/align`, `/ticket` (not `/to-tickets`), specs under `docs/spec/`. An existing spec for the work is developed in place; that is the 2026-09-06 decision, not a second file.
- `<project-local>` holds this project's spec path, tickets, and `AGENTS.md`. Source-project local blocks are not copied.
- Lifecycle archival stays `/maintain`'s; this skill writes and develops the spec.

## Stages

1. Record develop-in-place on the ticket. Write this RFC.
2. Write `.agents/skills/spec/SKILL.md`. Point `AGENTS.md` and `.agents/README.md` at the install.
3. First use: add the agreed loop-completion requirement to the existing draft. Do not mark the spec accepted.
4. Prove the ticket's boxes: catalog path, spec text, grep of `.agents` for `to-spec`/`to-tickets`, `AGENTS.md` installed list.

No tests: the artifact is instructions, as with `/ticket`. Proof is the files and the same loader path `.claude/skills` used for `/ticket`.

## Leave alone

`/plan`, `/implement`, `/verify`, `/maintain`, the pacer, `/edge`, architecture.md (still lazy), source projects.

## Proof

| Criterion | How it is shown |
|---|---|
| `/spec` under `.agents/skills/spec/`, catalog | File exists; `.claude/skills/spec/SKILL.md` resolves through the existing link |
| First use develops loop completion | The draft states the ring as an agreed requirement, in this file, not a second spec |
| No `to-spec` / `to-tickets` in installed files | grep of `.agents` |
| `AGENTS.md` lists `/spec` | local block `Installed:` line |
