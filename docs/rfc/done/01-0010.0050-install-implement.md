# Install /implement — implementation plan

**Authored:** 2026-09-06
**Last amended:** 2026-09-06

Implements [01-0010.0050](../../tickets/done/01-0010.0050-install-implement.md): install
the implementation mechanism — `/implement`, `/tdd` with its files, and
`/improve-comments` — and use `/implement` once to carry out this slice.

## Scope

Install three skill folders under `.agents/skills/`: `implement/`, `tdd/`
(SKILL.md plus `tests.md`, `refactoring.md`, `mocking.md`,
`interface-design.md`, `deep-modules.md`), and `improve-comments/`. First use
is this slice: after the bodies are on disk, follow `/implement` for the
remaining pointers (`AGENTS.md`, `docs/process.md`, `.agents/README.md`).
`/maintain` is not installed. `/verify` already is; do not reinstall it.

## Shapes

- Folder names `implement`, `tdd`, `improve-comments`; matching frontmatter
  `name`. Hosts discover them the same way they discover `verify`.
- `implement` is Meteoscape `implement` with selected names: `/tdd`,
  `/improve-comments`, `/verify` and `/maintain` at the handoff (not
  `/review-impl` or `/sync-arch`). The source line "run typechecking and the
  full test suite" is generalized: during the work, run the checks named in
  [Verification](../../process.md#verification); `/verify` is the verification of
  landed work. Do not inline commands. No `<project-local>` on `/implement`.
  Do not move the RFC to `done/`; that is `/maintain`'s.
- `tdd` is Meteoscape `tdd` plus its five files. The source cite of `/plan-impl`
  becomes `/plan`. An agreed RFC still satisfies its planning gate.
- `improve-comments` is Meteoscape `improve-comments`.
- `commit` is read from `AGENTS.md`; this project is `commit=ask`.
- Repair policy is [autonomy and repair](../../process.md#autonomy-and-repair).
  `/implement` follows the RFC; repair-and-report during the work is the
  `repair` switch, not a substitute for `/verify`.
- `/maintain` is named at the handoff and is not installed. A pass does not
  start a whole-tree maintenance.
- Instruction-install proof is the files and the loader path. The verification
  set is [Verification](../../process.md#verification); this project has none.

## Stages

1. This RFC. Record `Plan:` on the ticket.
2. Write the three skill folders. Point `AGENTS.md`, `docs/process.md` and
   `.agents/README.md` at the install.
3. First use: follow `/implement` for the remainder of this slice.
4. Prove the ticket's boxes: catalog paths for all three, first-use record,
   grep of `.agents` for `/review-impl` and `/sync-arch`, `AGENTS.md`
   installed list. `/verify` on this ticket is a later pass, after the install.

No tests. Proof is the files and `.claude/skills/{implement,tdd,improve-comments}/SKILL.md`
through the existing link.

## Leave alone

`/maintain` files; pacer; `/edge`; architecture.md; source projects; 0040's
`/verify` box; 0070. Do not reinstall `/verify`.

## Impact

**Main blast radius.** Three skill folders. Catalog, `AGENTS.md` installed-list,
`docs/process.md` installed-list, and `.agents/README.md` change. `/plan`'s
existing `/tdd` cites start to resolve. First use is this install.

**Hidden edges.** `/verify` already names `/improve-comments`; 0050 installs
that skill. Handoff names `/maintain` before that skill exists. No skill tests
exist to run. 0040 stays In progress until `/verify` runs on it.

**Leave alone.** `/maintain`, pacer, `/edge`, 0010.0010, source projects,
0040's remaining `/verify`.

**Recommendation.** Proceed, narrowed to the three folders plus first use on
this slice.

## Proof

| Criterion | How it is shown |
|---|---|
| `/implement`, `/tdd`, `/improve-comments` under `.agents/skills/`, catalog | Files exist; `.claude/skills/{implement,tdd,improve-comments}/SKILL.md` resolve |
| First use implements a planned slice | This install, following `/implement` after the bodies are on disk |
| Handoff names `/verify` and `/maintain`; no `/review-impl` or `/sync-arch` | grep of `.agents`; `/implement` names `/tdd` and `/improve-comments` |
| `AGENTS.md` lists `/implement`, `/tdd`, `/improve-comments` | local block `Installed:` line |
| `/verify` run on this ticket | later; not this RFC's install stages |
