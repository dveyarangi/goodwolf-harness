# A second mechanism is declared, and the shape either holds or is amended — implementation plan

**Authored:** 2026-09-07

Implements [01-0011.0022](../tickets/01-0011.0022-shape-survives-second-mechanism.md): `/maintain`
declared through the shape, its body rewritten as the rules its align grouped, the shape amended
where the second application broke it, and `docs/process.md` shedding what now has an owner.

**Planning state:** validated once; the one implementation challenge, the anchor repairs, is
resolved. Ready for a second validation pass.

## Governing boundaries

- [The spec](../spec/01-0011-mechanism-shape.md): one instruction file per mechanism; the doc is
  the instruction's why and the evidence the doc's why; `/maintain` applies and `/mechanism` owns;
  the register is derived; *ownership of shared parts is not a live question* while no script
  installs or extracts.
- [`/mechanism`](../../.agents/skills/mechanism/SKILL.md) and
  [`MECHANISM-FORMAT`](../../.agents/skills/mechanism/MECHANISM-FORMAT.md): the directory, the
  doc's sections, the two tables, the moment kinds, and what `mechanisms.py` rules on. Every
  section this plan writes is that format's; nothing is restated here.
- [`AGENTS.md`](../../AGENTS.md) *Core and instance*: nothing under `.agents/` references a file in
  `docs/` outside a `<project-local>` block; a `<temporary>` block is exempt.
- [The architecture](../architecture.md): the mover's contract under *Interruption and recovery*
  and the temporary-statement contract stay where they are; the *Responsibilities* bullet for
  `/maintain` changes with this slice.
- The ticket's own decisions, all dated 2026-09-07: `installed`; the moments table with two
  `embedded` rows; the four things and landing-time agreement being `/verify`'s; rules A1–E3 with
  two strengths; meta-rules never injected; installed text, never pointers; no trigger row; the
  mover is `/ticket`'s; the clock is [.0060](../tickets/01-0011.0060-mechanism-rechecked-when-governing-moves.md);
  two graders.

## Impact

Assessed on the shapes below, after the align's own `/impact` on the ticket. Verdict: **proceed**;
one open choice, the anchor repairs, is put to `/align` rather than decided here.

**Must change.** `.agents/skills/maintain/SKILL.md` (rewritten); `.agents/mechanisms/maintain/`
(new, one file); `docs/mechanisms/maintain.evidence.md` (moved from
`docs/research/maintenance-findings.md` and sorted); `.agents/skills/mechanism/SKILL.md` (three
amendments, two landed at `/plan`); `.agents/mechanisms/mechanism-shape/mechanism-shape.md` (relied-on owners, and *Not
yet at the shape*); `docs/mechanisms/mechanism-shape.evidence.md` (two refutations);
`.agents/glossary.md` (four terms); `docs/process.md` (lines 21, 67–101); `docs/architecture.md`
(line 6, *Responsibilities*, the evidence path); `.agents/scripts/move_doc.py` (docstring);
`tests/test_mechanisms.py` (two cases); the queue and the ticket.

**Anchors, the widest edge.** Deleting `#tree-maintenance`, `#mechanical-maintenance` and
`#current-documents-and-evidence` from `docs/process.md` orphans citations in seven records:
`docs/architecture.md:6`, `docs/research/entry-contract-findings.md:62`, `01-0010` (lines 28, 40,
222–226, 250), `done/01-0010.0070` (six), `rfc/done/01-0010.0070` (two), `done/01-0010.0090`
(two), `01-0017:71`. The mover repairs paths, never fragments (`docs_corpus.Target` parses them
and `move_doc` carries them through unchanged), and each citation's new home differs — the
policy split into `/maintain`'s doc, `/mechanism`'s body and the glossary — so the retarget is a
per-citation judgment, not a derivation. The first slice took the same road for four citations and
reported them as hand edits. This is the one implementation challenge; see *Open for `/align`*.

**Must verify.** `mechanisms.py --check` over two declarations reporting both true, with an
`installed` state and two `embedded` rows on real material — neither has a test today, and the
`embedded` branch (`mechanisms.py:371`) has never run outside the suite. `temporary_statements.py`
over `.agents` listing the skill body's block: its documented scope in the body omitted `.agents`
until this slice, so the first run over the new scope is itself a finding either way. The move
through `move_doc.py` to a **different basename**: `_malformed` checks markdown, repository
bounds, membership and duplicates, and nothing about basenames, so the rename is inside the
contract; the architecture's *preserves basenames* sentence describes paired close, not the
mover's limit.

**May simplify.** The body loses the commit sentence, the evidence pointer and the *Identify*
step's links into `docs/`; three sections of `docs/process.md` go and line 21 becomes one
sentence.

**Hidden edges.**

- **A `<temporary>` block in a skill body is the first of its kind.** `AGENTS.md` permits it
  anywhere, the enumerator reads any markdown it is pointed at, and `--remove` takes `FILE:LINE`.
  What was never true is that a pass looked there: the body said `docs AGENTS.md`. The block
  would have been invisible to the command meant to report it.
- **`/mechanism` cannot shed R1–R6 yet.** The rules file is `.0020`'s; removing the prose before
  the file exists leaves R1 with no home. So the *installed text* amendment lands as the rule that
  bodies restate nothing, with the *Records* section explicitly marked as staying until `.0020`.
  `/verify` must read that as intended, not as a violation.
- **Three claimants on `docs_corpus.py` and `tests/harness.py`** — `mechanism-shape` says nobody
  removable, this doc says `/ticket`'s gap, `.0025` will say its own. The spec's line that
  ownership is not live while nothing installs or extracts is what makes this harmless, and both
  docs cite it rather than re-arguing.
- **The architecture's `/verify` and `/maintain` bullets overlap after A2** unless `/maintain`'s
  is cut to drift scope; the plan cuts it.
- **`docs/README.md`** curates entry points and does not list `docs/research/`'s evidence files
  or `docs/mechanisms/`; nothing there changes, as the first slice already concluded.

**Leave alone.** `move_doc.py`, `temporary_statements.py`, `docs_corpus.py` and `tests/harness.py`
behaviour; `mechanisms.py` code — the two new tests exercise paths that exist; `/verify:38` and
`/implement:38` (01-0017's); *Autonomy and repair* and *Verification* in `docs/process.md`;
`.agents/README.md`; `.0020`'s delimiter and rules file; the clock.

**Challenge.** The problem is demonstrated twice on 2026-09-07: a pass following the body missed
a stale roster, and the shape had one application. The plan is not narrower than the ticket's
acceptance list; every file above is named by a criterion or a decision. The one abstraction, the
four things, is the ticket's and was justified there by two shapes.

## Shapes

### The declaration — `.agents/mechanisms/maintain/maintain.md`

To the format, section by section. What the align settled, in the words the doc will carry:

```md
# maintain — holds documentation, implementation and records in agreement, and repairs the drift

- **instruction** `.agents/skills/maintain/SKILL.md` — the pass: scope, the four things, temporary statements, archiving, finish
- **state** installed
<project-local>
- **evidence** `docs/mechanisms/maintain.evidence.md`
- **declared by** `docs/tickets/01-0011.0022-shape-survives-second-mechanism.md`
</project-local>
```

**How it works** — prose: the four things; that a pass is drift-scoped and landing-time agreement
is `/verify`'s; that it is `installed`, so the parts table is what an installer adds, and what a
tree loses without it: the ring still runs, unmaintained. That it is reached from the ring in
`AGENTS.md` and from `/verify` naming it, with no trigger part — `/implement`'s two sentences
naming it are an artefact of the `/sync-arch` era *(the user, 2026-09-07)*, removed in 01-0017's
sweep and not relied on here. That its record
is the marks it does not have yet, with the reason and the ticket.

**Moments** — the table as aligned, one row each, in this order. Links here are relative to this
file; the doc writes them relative to its own directory:

| moment | instructed by | kind, and why |
|---|---|---|
| declaring a scope and running a pass over it | `.agents/skills/maintain/SKILL.md` | |
| re-checking derived work when what governs it moved — a doc against the meta-rules, an implementation against its doc, records against their format | `.agents/skills/maintain/SKILL.md` | |
| holding a landed slice to its governing docs, both ways | — | elsewhere — landing-time agreement is verification, `.agents/skills/verify/SKILL.md` |
| knowing a re-check is due | — | not yet — nothing gives *since* a meaning until the marks exist, [.0060](../tickets/01-0011.0060-mechanism-rechecked-when-governing-moves.md) |
| deciding whether a temporary statement's condition holds, and retiring the block | `.agents/skills/maintain/SKILL.md` | |
| cleaning prose and records to one home per fact | `.agents/skills/maintain/SKILL.md` | |
| closing a finished ticket with its RFC | — | embedded — `/ticket`'s paired close, sitting in this body until its rules file installs it, `.agents/skills/maintain/SKILL.md` |
| updating the header and queue row at close | — | embedded — `/ticket`'s record shape, same home, same repair, `.agents/skills/maintain/SKILL.md` |
| checking a mechanism's records against their declared format | — | not yet — the maintainer-script rule reaches this skill by injection and no installer exists, [.0020](../tickets/01-0011.0020-rules-one-home.md) |
| repairing mechanically, history included | — | elsewhere — a meta-rule, read where it lives, `.agents/skills/mechanism/SKILL.md` |
| disposing of a concern | — | not yet — the concern index has never existed, [01-0017](../tickets/01-0017-io-graph-coherent.md) |
| checking links outside a close | — | not yet — the only link check is the mover's note over records a close rewrote, [01-0017](../tickets/01-0017-io-graph-coherent.md) |
| cleaning inline comments in scope | — | elsewhere — `/verify` applies `/improve-comments` to landed work, `.agents/skills/verify/SKILL.md` |
| running the verification set the scope touched | `.agents/skills/maintain/SKILL.md` | |
| installing this mechanism into a tree, and removing it | — | not yet — no installer exists, [01-0010](../tickets/01-0010-dev-harness-shared-and-local.md) |
| telling a mechanism at inception what to hand `/maintain` | — | not yet — the rule is authored in `maintain.rules.md` targeting `/mechanism`'s *Incept* and no installer carries it there, [.0020](../tickets/01-0011.0020-rules-one-home.md) |
| picking up a dream | — | unowned by design — the entry file says *may*, a permission and not a duty, while `/dream` is experimental |

`embedded` rows name where the instruction sits, as the format says, and that is this body. The
check resolves the referent as a path (`mechanisms.py:371`), so the row is checkable today.

**Install adds, uninstall removes**

| part | where |
|---|---|
| instruction file | `.agents/skills/maintain/SKILL.md` |
| this doc | `.agents/mechanisms/maintain/maintain.md` |
| its rules file | `.agents/mechanisms/maintain/maintain.rules.md` |
| the enumerator | `.agents/scripts/temporary_statements.py` |
| its tests | `tests/test_temporary_statements.py` |

The rules file holds one rule, targeting `/mechanism`'s *Incept*; the format finds it by name
and the check accounts for it in the directory. It is authored here and installed by nobody until
`.0020`, which *What it produces* says.

**Relies on, and does not own**

| part | where | owner |
|---|---|---|
| the mover | `.agents/scripts/move_doc.py` | `/ticket`'s mechanism, from `01-0011.0025`; a declared gap until then |
| the mover's tests | `tests/test_paired_close.py`, `tests/test_refusals.py`, `tests/test_citations.py`, `tests/test_command_line.py`, `tests/test_failure_contract.py` | same |
| citation reader | `.agents/scripts/docs_corpus.py` | same gap; `01-0011.0025` claims it |
| test harness | `tests/harness.py` | same |
| the shape check | `.agents/scripts/mechanisms.py` | `mechanism-shape` |
| the method's vocabulary | `.agents/glossary.md` | nobody removable |

The check reads `where` as one path; the five test files go on five rows, not one.

**What it produces, and who reads it** — the pass's report, read by the person who asked for the
pass and by `/conclude`; repaired files, read by whoever reads them next; moved records, read
through their repaired citations; a retired temporary statement's replacement sentence; and
**the rules file**, read by the installer alone — which does not exist, so today nothing reads
it, and the doc says so rather than leaving the reader implied. Nothing else: no record until the
marks, no index.

**Not yet at the shape** — the marks; the two `embedded` rows; the four `not yet` rows; and that
`/maintain` has never been run against a mechanism other than the one that wrote the shape.

**What retires this** and **What would show it working** — the ticket's words: retirement when
every agreement is held by a script in the verification set; two graders, two questions.

### The body — `.agents/skills/maintain/SKILL.md`

Frontmatter unchanged except the description, which gains *drift* and loses *rules, contracts*:
the four things in one sentence. Then, in this order, the groups as the align named them, each a
short heading and rules one per line at the ticket's length or less, each carrying its provenance
inline in the form `/mechanism` uses, *(the user, 2026-09-06)*:

1. **What this holds, and when** — A1, A2, A3, A4. A4 reads *dueness is read from the clock,
   never inferred*, immediately followed by:

   ```md
   <temporary until="01-0011.0060 is done" ticket="docs/tickets/01-0011.0060-mechanism-rechecked-when-governing-moves.md">
   There is no clock. Every declared mechanism in the scope is due at every pass: re-read its doc
   against `/mechanism`, its instruction file against its doc, and its records against their format.
   </temporary>
   ```

2. **Declare the scope** — B1, B2 with the capture-and-route sentence, B3.
3. **Judgment stays here; scripts rule on form** — C1, C2, C3; C4 is **not** written, because it
   arrives by injection and the align forbade a prose substitute. A one-line pointer at the
   `not yet` row is also not written: the doc carries the gap.
4. **Repair** — D3, D4. D1 and D2 are not written; they are `/mechanism`'s and read there.
5. **One home per fact** — E1, E2, E3.
6. **Temporary statements** — the enumerator's commands as today, with the scope corrected to
   `docs AGENTS.md .agents`, and the removal round.
7. **Archive** — the mover's commands as today. This section is the `embedded` material and says
   so in one sentence: *these are `/ticket`'s rules, here until its rules file installs them*.
8. **Finish** — run the set; report; *`/maintain` is not `/verify`*. The commit sentence goes.

Closing `<project-local>` block, the only place a `docs/` path appears: the verification set
(`docs/process.md#verification`) and the architecture's two contract sections the body leans on
(`docs/architecture.md#interruption-and-recovery`, `#temporary-statements`). The evidence pointer
leaves the body: the doc names it.

Every rule that `docs/process.md` states today and this body will state tomorrow appears once,
here. Nothing in the body cites the process document for policy.

### The shape amendments — `.agents/skills/mechanism/SKILL.md`

Three. Two **already landed at `/plan`, on the user's instruction**, with their causes in
`mechanism-shape.evidence.md` under *Refuted by the second application*:

- Under *Is it a mechanism*: ~~rules it needs elsewhere are injected pointers~~ → *installed as
  text from its rules file, and no body restates one*. **Landed.**
- Under *Rules, injection and retraction*: *a sentence about another mechanism is that
  mechanism's* — what shapes how mechanisms are built around it is a rule in its rules file,
  installed there and never authored there; its core function reaches a session through its own
  description, strengthened at tier 1 where warranted, and a harness-wide principle may land in
  `AGENTS.md` by that mechanism's rare decision. **Landed.** The consequence for this slice: the
  sentence telling a mechanism what to hand `/maintain` is **not written into *Incept***. It is
  `/maintain`'s, authored in `maintain.rules.md` with `/mechanism`'s *Incept* as its target, and
  it reaches `/mechanism` when `.0020`'s installer exists. Until then `/maintain`'s doc carries
  *telling a mechanism at inception what to hand `/maintain`* as `not yet` naming `.0020`, and
  `/mechanism` carries no prose substitute.

The third, this slice's, one section in place:

- A new two-line section after *Rules*, **Mechanical by construction**, holding D1 and D2 in the
  entry-file voice: *A mechanism is designed from inception for mechanical derivation, checking
  and repair. Anything that can be maintained mechanically is; a missing script is written as
  part of the repair; archived records are not exempt.* Provenance *(the user, 2026-09-05)*. And
  the *Records* section's prose gains the single clause *— in this body until the rules file takes
  it at `.0020`*, so a reader sees the exception is declared rather than missed.

`mechanism-shape.md`: the relied-on owners for `docs_corpus.py` and `tests/harness.py` change to
`/ticket`'s gap, citing the spec's line; *Not yet at the shape* drops *one declared mechanism is
weak evidence* and gains nothing. Its evidence already carries the three refutations.

### The glossary — `.agents/glossary.md`

Four entries, to `GLOSSARY-FORMAT`, defining what a thing **is**:

- **Documentation** — the role a doc plays for an implementation: what it must do and why. A
  mechanism's doc for its skill and scripts; a project's architecture for its code.
  *Avoid*: spec, manual.
- **Implementation** — what a documentation describes and must agree with: a mechanism's
  instruction file and scripts; a project's code. *Avoid*: source, product as a synonym.
- **Record** — a row a mechanism writes under a declared format: a ticket, an RFC, a session
  record, a mark. Live until archived. *Avoid*: log, entry.
- **Working rule** — a rule a session wrote under the repair policy or that came with a selected
  skill; a maintainer may amend it under repair-and-report, recording the cause in the evidence.
  Contrast **Strict rule**.

**Doc** keeps its entry and gains one clause: *the documentation of a mechanism*.

### The process document — `docs/process.md`

- Line 21 becomes one sentence: `/maintain` is declared at `.agents/mechanisms/maintain/`, and its
  rules are its body's.
- Lines 67–71: the `<temporary>` block, removed through `temporary_statements.py --remove` with
  its fingerprint; the heading goes with it and nothing is written in its place, because the
  section's content has been `/mechanism`'s since `.0010`.
- Lines 73–93: *Tree maintenance* and *Mechanical maintenance*, deleted. Their rules are the
  body's A1–E3 and `/mechanism`'s meta-rules; their reasons are the two docs'.
- Lines 95–101: *Current documents and evidence*, deleted — session nine routed it here as a
  duplicate of the doc-to-evidence chain `/mechanism` owns. **This widens the ticket by one
  section**, and the ticket gains the line.

### The architecture — `docs/architecture.md`

- Line 6: *maintenance policy belongs in the process* → *maintenance policy is `/maintain`'s
  body, declared at `.agents/mechanisms/maintain/`*.
- *Responsibilities*, the `/maintain` bullet: the four things at drift scope, in one sentence, and
  the evidence path repaired by the mover.
- Nothing else. The contract sections are D4's and C2–C3's durable home and stay.

### The evidence — `docs/mechanisms/maintain.evidence.md`

Moved by `move_doc.py docs/research/maintenance-findings.md docs/mechanisms/maintain.evidence.md`,
which repairs `architecture.md:31` and the two RFC/ticket citations. Then sorted: the two 2026-09-06
probe sections are evidence and stay; a new opening states what the file is, and a new section
*What the 2026-09-07 align refuted* carries `none by property`, the three-subject split, and
*injected pointers*, each in two sentences with the ticket as the full record. Nothing that
explains why the *instruction* is what it is exists in the file today, so nothing moves to the doc.

### The tests — `tests/test_mechanisms.py`

Two cases in existing classes, synthetic fixtures only:

- `TheHeader.test_accepts_installed_as_the_other_state`: a doc with `- **state** installed` is
  reported clean.
- `AMomentsRow.test_saying_embedded_names_the_body_the_instruction_sits_in`: an `embedded` row
  naming a path that exists is clean; one naming a path that does not is reported *does not
  resolve*.

Both pass at first write against the existing code. Per the shape's rule, each is watched failing
by breaking the branch it exercises, and the break reverted, before the stage ends.

### The docstring — `.agents/scripts/move_doc.py`

*the archive half of `/maintain`* → *`/ticket`'s paired close, run from `/maintain`'s pass*.

## Stages

Each stage ends with the whole verification set green and a positive count.

1. **Vocabulary and shape first.** Glossary's four entries and the **Doc** clause;
   `/mechanism`'s remaining amendments — the *Mechanical by construction* section and the
   *Records* clause, the other two having landed at `/plan`; `mechanism-shape.md`'s owner rows,
   its new `not yet` row for what `/maintain` must be handed, and *Not yet*;
   `mechanism-shape.evidence.md` already carries the refutations. Run `--check`: one declaration,
   still true.
2. **The tests.** The two cases, each watched failing by a deliberate break, then green.
3. **Declare `/maintain`.** Move the evidence through the mover; sort it; write the doc and
   `maintain.rules.md`. Run
   `--check`: **two declarations, both true**, and `--index` renders a `maintain` row. This is the
   stage the ticket exists for: if the check has to change to admit the doc, stop, record what
   broke in `mechanism-shape.evidence.md`, and amend the shape before continuing — never the doc.
4. **The body.** Rewrite `SKILL.md` as above. Run `temporary_statements.py docs AGENTS.md .agents`
   and confirm the block is listed with its ticket. Correct the docstring.
5. **The shedding.** `docs/process.md`: retire the block through `--remove`, delete the three
   sections, rewrite line 21. `docs/architecture.md`: line 6 and the bullet. Repair the anchor
   citations per the `/align` outcome below, and report every one. The enumerator reports two
   statements. Update the queue's test count, the ticket's criteria with dates, and its line for
   *Current documents and evidence*.

## Resolved at `/plan`

**The anchor repairs, fourteen citations in seven records — hand edits, reported** *(the user,
2026-09-07)*. The two roads, kept for the record:

- **Hand edits, reported**, as `.0010` did for four. Each citation gets the home its subject went
  to: `#tree-maintenance` → `/maintain`'s doc; `#mechanical-maintenance` → `/mechanism`'s new
  section; `#current-documents-and-evidence` → `/mechanism`'s *Three homes*. Historical records
  keep their recorded facts and gain a pointer to where the policy now lives, which is what the
  mover does for paths. Cost: fourteen edits, one report.
- **An anchor mode for the mover**, which D1 would ask for if the repair were derivable. It is not:
  three anchors go to three homes, and one of the seven records cites two of them in one line.
  The script would take the mapping as an argument and be right by construction only for this
  slice. `.0025` has already said the mover does not change there.

Hand edits it is, with a one-line note on [01-0017](../tickets/01-0017-io-graph-coherent.md)
that a link check which resolves fragments would have listed all fourteen before anyone counted
them.

## Limitations and follow-ups

- **R1–R6 stay in `/mechanism` as prose** until `.0020` writes the rules file. The body marks the
  exception; `/verify` reads it as declared.
- **Two `embedded` rows and four `not yet` rows** are the honest state, retired by `.0025`,
  `.0020`, `.0060`, 01-0017 and 01-0010 respectively.
- **The body is the first skill written as addressable rules.** One shape; `/skill-up`'s owed
  change on [01-0010.0100](../tickets/01-0010.0100-remaining-named-corpus.md) decides whether it
  generalises.
- **`/verify:38` restates D1** and stays until 01-0017 sweeps the corpus.
- **The third grader question cannot be answered by this session**: whether `.0025` lands against
  the doc as written is `.0025`'s to report.
- ~~An Edge record over the maintenance seam, after this lands.~~ **Not an edge** *(the user,
  2026-09-07)*: maintenance is one instance of the general seam of mechanism construction — what a
  mechanism asks of other mechanisms — and the mechanism doc already is that record. What must
  still land is the **instruction**: at inception, a mechanism writes what `/maintain` must do for
  it — a maintainer script to run, records to re-check — as rules in its rules file targeting
  `/maintain`, and nothing is written into `/maintain` by hand. ~~That is the third shape
  amendment, one sentence in `/mechanism`'s *Incept*.~~ **Corrected** *(the user, 2026-09-07)*:
  a sentence about another mechanism is that mechanism's, so the instruction is
  `maintain.rules.md`'s, targeting `/mechanism`'s *Incept* — Life's arrangement, `maintenance`
  installing into `/mechanism` at its `SKILL.md:340` — authored in this slice and installed by
  `.0020`. See *The shape amendments*. Its first use is `.0025`, `ticket.rules.md` targeting
  `/maintain`.
- **The map of missing instructions** *(the user, 2026-09-07)*. Every `not yet` and `embedded`
  row here, and every one the later declarations will carry, is maintenance owed to a skill that
  has not been through `/mechanism`'s inception. [.0050](../tickets/01-0011.0050-shape-checked.md)'s
  report across every declared mechanism **is** that map; [01-0017](../tickets/01-0017-io-graph-coherent.md)
  owns the homes. The user's refinement, recorded on both: an absence should say which fix it
  wants — a rule injected from a mechanism not yet declared, a rule that cannot be injected and
  needs a body, or a genuine gap.

## Proof

- The verification set green with a positive count at every stage; `mechanisms.py --check`
  reporting two declarations true at stage 3 and after.
- `--index` rendering `maintain` beside `mechanism-shape`, written nowhere.
- Both new tests watched failing before passing.
- `temporary_statements.py docs AGENTS.md .agents` listing the skill body's block with its ticket
  after stage 4, and two statements in total after stage 5.
- No `docs/` citation in `.agents/skills/maintain/SKILL.md` outside its `<project-local>` block;
  no citation anywhere to the three deleted anchors.
- The evidence file at its new path, cited from the doc's `<project-local>` block, with
  `architecture.md:31` repaired by the mover.
- The acceptance list on the ticket checked with dates, including the one it gains.
