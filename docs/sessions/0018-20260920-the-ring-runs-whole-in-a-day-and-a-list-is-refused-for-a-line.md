# The ring runs whole in a day, and a list is refused for a line

Session eighteen, 2026-09-20, one morning into one afternoon. It opened with "good morning" and
the first reply ran `/recall` unasked — the wake rule's grade, and it passed. Then two aligns,
a plan validated three times, an implementation, a verify and a close, all on
[01-0011.0050](../tickets/done/01-0011.0050-shape-checked.md): the first ticket to go through the
whole delivery ring in a single session. Five commits of mine — `ALIGN`, `ALIGN`, `PLAN`,
`IMPLEMENT`, `MAINTAIN` — and this one; no push.

## Work completed

Details are on the records named.

- **`.0140`'s align** ([the ticket](../tickets/done/01-0010.0140-core-stands-alone.md)). A **leak**
  — a citation from core to a document only the instance has — is now a method-glossary term.
  No exemption list: instance-owned blocks are skipped mechanically, a `<straw-dog>` exempts
  nothing that ships, and a path under `docs/` is legitimate only as a **place** — a record some
  mechanism declares, the directory or the declared file, never a record inside it. Landed in
  [*Core and instance*](../../AGENTS.md#core-and-instance), [ADR-0002](../adr/0002-core-may-not-depend-on-the-instance-half.md)
  and the install spec's test bullet, all of which had said a straw dog was exempt. The check
  folds into `mechanisms.py --check` as mechanism-shape's part; `docs/process.md` is the pacer's
  straw dog whole and `.0140` moves its harness rules into core.
- **`.0050`'s align, plan and delivery** ([the ticket](../tickets/done/01-0011.0050-shape-checked.md),
  [the RFC](../rfc/done/01-0011.0050-shape-checked.md)). A `not yet` row is a straw dog written
  as one — body the kind, reason in `until`, ticket in the binding, which the shear strips.
  A skill no declaration names opens its body with `Mechanism: not yet`, wrapped and bound the
  same way; the reverse pass reads it and fails on a skill that neither is named nor claims, or
  both, or is named twice. R6 installed into `/skill-up` and `/mechanism`. Nine tests, 228 green;
  53 straw dogs listed, no diagnostics; the mover carried the eleven rows' bindings end to end.
- **Rule failure 5**, opened and closed in one pass ([the register](../rule-failures.md)): an
  allowlist file was built and refused the same hour. R4 now names a file of what other files
  say for themselves as an index whatever it is called; R6 no longer says *allowlist*.
- **Failure 4 graded** — the wake rule fired on this session's first reply.
- **Concern 2**'s membership trigger retired at `/verify`; `.0140` and `.0130` no longer wait on
  `.0050`; the queue's candidate is `.0140`'s `/plan`.

## What the user corrected, kept as evidence

- **"Links from straw dogs are violations too."** I had carried the entry file's exemption into
  the align as settled. The shear ships what a wrapper wraps; the exemption only moved the
  failure to the port.
- **"process.md is pacer's, right? if it contains rules, it must not live in docs. This entire
  file is a straw dog."** I had proposed an anchor rule to split one file into a good half and a
  bad half. The file is provisional whole, and its rules move to core.
- **"Each one of those belongs to future mechanisms, right? they are their outputs?"** My
  convention list dissolved into the mechanism shape's own records declaration — a place is what
  a mechanism declares.
- **"Too ambiguous. Names matter. Do we have a rule that names matter?"** For terms, `/align`'s
  glossary rule, which I had skipped when I coined *reaches*; *leak* was already the entry
  file's word.
- **"Perhaps we should use straw dog semantics?"** — then **"body is `not yet`, 'the pacer owns
  it' belongs to the tag."** The reason is the origin's, not the row's.
- **"Way too much deliberation in mechanism-format. WRITE FOR CAPABLE MODEL!!"** The shelf's
  additions went from thirty lines to eight.
- **"What the heck is going on with the allow list? The idea was to inline the straw dogs into
  the targets."** I had read "one file" as a register. Rebuilt inline; rules amended.
- **"Why 'Mechanism' label outside the tag?"** A straw dog wraps what its ticket will change,
  and the declaration removes the whole line. The label went inside.
- **"Did you actually run the leak check?"** No — it is `.0140`'s and unbuilt; I said so.
- **"Yet you still left a lot of doc references unwrapped, why?"** Sixteen bare links from core
  into `docs/`, all of which `.0140` will change, all unwrapped: failure 3's shape again. Not yet
  struck — the user chose to close the slice first.

## Open, with owners

- **The sixteen bare links** — `.0140`'s. Wrap them as straw dogs at its `/plan`, link into the
  attribute and the ticket named in words: five bound to the live tickets they name in
  provenance prose, eleven bound to `.0140` (`process.md` ×4, evidence ×2, closed-ticket story
  ×4, `discover/EVIDENCE.md` ×1). Then strike failure 3 a second time on the register: the rule
  did not fire on text I was reading rather than writing.
- **`.0140`'s `/plan`** is the queue's candidate, `next-cycle=ask`. Its decisions are all on the
  ticket; the holding list of `docs/` places is the check's own data under a `TODO`.
- **For `.0130` to inherit**, stated on `.0050`'s RFC: the shear must strip an inline wrapper
  (a table cell's, a skill's first line's) as well as a block one; a sheared `not yet` is
  unbound and only the origin's check may fail on it — the `TODO` in `mechanisms.py` names
  the ticket; the install spec's step 2 says the manifest "reads the allowlist" and there is
  none — what ships is every skill directory.
- **01-0017's nineteen claims** are the map of declarations owed, now readable in each skill's
  first line; `/recall`'s binds to 01-0020 and `/edge`'s to 01-0010.0100, as the queue routes.
- **Two guesser candidates** in `mechanism-shape.md` (*until then*, *hand-fixed until*) are older
  prose, judged claims twice today; a third look is `/maintain`'s.
- **Carried from seventeen, unchanged:** 01-0020's debt of a rules file for the wake rule and
  the entry file's third claimant; the entry file's straw-dog sentence on `.0130`; contribution
  back; the user's grade on the mechanism shape; frost_map's `tickets.py` unrun.

## Housekeeping

`301cf94` at close, five commits ahead of `origin/main`, unpushed (`push=ask`), tree clean
before this record. Verification set green: 228 tests at ~60s, three checks clean; 53 straw
dogs, all bound, no condition met. Entry contract v9.
