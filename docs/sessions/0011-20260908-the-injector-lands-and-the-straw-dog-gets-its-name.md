# The injector lands, and the straw dog gets its name

**Date:** 2026-09-08.
**Historical handoff:** current state belongs to the [delivery queue](../tickets/README.md) and
the owning tickets. This record is a snapshot of the eleventh session, in Claude Code, the
second written by `/conclude`. Wake was by hand — `/recall` is not installed — and opened at
the user's suggestion with a `/maintain` pass scoped to `/mechanism`, the reflexive case.

## Work completed

Two full rings in one day, each `/align` → `/plan` → `/implement` → `/verify` → `/maintain`,
plus the opening maintenance pass and a ticket minted between them. Fourteen commits.

- **`/maintain` on `/mechanism`** ([queue](../tickets/README.md)): the check was clean and the
  drift was in prose the check never rules on — the doc said the shape injects no rules while
  `/maintain` had carried five of its rules by hand since the evening before. Repaired; the
  moment for retiring a mechanism added.
- **[01-0011.0020](../tickets/done/01-0011.0020-rules-one-home.md), the injector**, closed:
  `inject_rules.py`, one block per mechanism per target under `<installed by=>`, two rules files
  to the shelf's grammar, three blocks installed into `/maintain` and `/mechanism`, the entry
  contract at v4 with the hand-edit sentence, `--check` in the verification set. Three `/verify`
  passes; the tool met real drift on the live tree at the user's question. 115 → 160 tests.
- **[01-0011.0070](../tickets/done/01-0011.0070-straw-dogs-marked-and-found.md), the straw
  dogs**, minted from the injector's verify and closed the same afternoon: `<temporary>` is
  `<straw-dog>` and the enumerator `straw_dogs.py`; the entry contract is v5 and instructs the
  writer; the script guesses where an unwrapped one stands and reads a `TODO` naming its ticket
  as one; `/verify` reads landed work against the pending tickets in a sentence that is itself
  the first straw dog under its rule. The first guess over the whole tree wrapped six and
  repaired two. 160 → 173 tests; four straw dogs listed → eleven.
- **The queue stopped narrating**: its chronological pass paragraphs, which restated what
  tickets and session records own, are gone; 150 lines to 62.

## Settled decisions and their owners

All on the closed tickets. The ones that reshaped more than their slice:

- **Batch, not per rule** *(the user, at `/plan`'s second pass)*: one installed block per
  mechanism per target, re-installed whole when its source moves. Dissolved the per-rule tag,
  the ordering rule for blocks sharing an anchor, and most of the *already present* question.
- **Meta-rules are never injected**, so `/verify`'s review sentence is its own, and the
  hand-edit prohibition is one sentence in the entry file rather than a rule installed twice.
- **A straw dog has a named successor** *(the user)*. What has none is a claim and is not
  wrapped. Not everything starts as one: a `not yet` row declares an absence and a working
  record is provisional by status — a straw dog lives only in a surface read as truth.
- **A TODO is the marking in code** *(the user)*: naming its ticket, a straw dog the tool lists;
  naming none, a guess.
- **Guessing is a mode, not part of every listing**, because the rule's own section says
  *until* twice and would be re-reported forever.
- **Which mechanism owns straw dogs is 01-0016's**, and `/verify`'s sentence is wrapped until
  that align answers.

## Open, with owners

- **Next cycle needs a nod** (`next-cycle=ask`). The queue names
  [.0025](../tickets/done/01-0011.0025-archive-duty-reaches-maintain.md): the ticket mechanism, the
  first record shape with a maintainer script, and the first slice to use the injector without
  having built it — the grader `/maintain`'s doc pre-registers.
- **The user's grade on the shape** is still not given: did the shape change because `/maintain`
  did not fit it, or was the declaration bent? Pre-registered on both mechanism docs since
  yesterday.
- **R6** waits on [.0050](../tickets/done/01-0011.0050-shape-checked.md), which creates the
  allowlist it names.
- **Two candidates the guess cannot wrap**: table cells bound in their own words. And what it
  cannot find at all: a claim that became false with no tell — *owns none* — which is the limit
  `/verify`'s review exists for.
- **A recipient's own word list** → a facet on
  [01-0010.0110](../tickets/01-0010.0110-project-facets-injected.md), if ever wanted.

## Session through the advise questions

**Great:** the user's *"why not use HTML tag as everywhere else?"* and *"we do not need per
rule, all rules can come together, no?"* — two questions that each removed a mechanism from the
plan. And *"TODO is a straw dog, no?"*, which dissolved a wording question into a concept.

**Good enough:** running the injector against real drift on the live tree rather than only
fixtures, at the user's prompt; and running the guess over the tree as it stood at `9ca9a09` to
check the plan's load-bearing assumption before writing the RFC.

**Questionable:** twice today the code preceded most of its tests, and a green first run was
read as proof. The first mutation of the day never applied — a double-quoted shell pattern
turned escapes into a carriage return — and a passing suite was taken as a caught break until the
line was looked at. The evidence records it as what it is.

**Missing:** the align premise *three forms unified* survived until the user asked why a
`not yet` row is a straw dog. It is not, and the question should have been asked before the
premise was written.

**Redundant:** the first RFC of the day grew while the design shrank, because it carried story.
Cut from 415 lines to 284 when the user asked why.

**Out of balance:** the day's second ring was aligned, planned three times, implemented,
verified and closed inside three hours. The plan's third pass found real gaps each time; a
fourth might have too.

**Hidden edges:** a replace-all on `_OPENING` rewrote the `INSTALLED_OPENING` I had just
imported. The mover re-aims links inside the queue before a queued prose edit lands, so an edit
prepared against the old text fails. Both cheap, both worth knowing.

**Easier:** the queue's current-stage paragraph is rewritten by hand at every close; .0040
derives the table, and the paragraph could be shorter once it does.

**Next:** [.0025](../tickets/done/01-0011.0025-archive-duty-reaches-maintain.md), on the user's nod.

## Housekeeping

Verification set green at every commit: 173 tests, `mechanisms.py --check` over two
declarations, `inject_rules.py --check` with three blocks present, the listing at eleven straw
dogs, all bound, no condition met. Zero dangling citations at both closes. The entry contract
moved twice, v3 → v5; the next session's first line changes.
