# The local file lands, and the candidate turns out to be a chain

Session twenty, 2026-09-20, the evening after [session nineteen](0019-20260920-core-stands-alone-and-a-straw-dog-learns-to-stand-alone.md).
It opened with "hello", ran `/recall` first by rule, took a whole-tree `/maintain` on the user's
word, spent three questions on how the queue's candidate had been chosen, parked two decisions on
the pacer ticket, and then took [01-0010.0110](../tickets/done/01-0010.0110-project-facets-injected.md)
through the whole ring — three `/plan` passes, `/implement`, `/verify`, `/maintain` — the third
ticket to run it whole in a day. Commits of mine: `MAINTAIN`, `ALIGN`, `PLAN`, `IMPLEMENT`,
`VERIFY`, `MAINTAIN`; this record is uncommitted at writing (`commit=ask`); nothing pushed.

## Work completed

Details are on the records named.

- **The whole-tree `/maintain`.** Two `not yet` rows had been instructed all along: `/ticket`
  has instructed concern promotion since it was installed and `/align` instructs a concern's
  disposal; both rows were declared `not yet` because the concern index did not exist, not
  because the instruction was missing. Three claims true on one date left the mechanism docs for
  their evidence.
- **How the candidate was chosen** — it was not. The status page's candidate line was a chain
  advanced by the closing session, the ordering rule applied once in the tree's history, three
  carriers of order disagreeing. Recorded on [01-0020](../tickets/01-0020-pacer.md) with the
  shape the user agreed in principle: order moves when delivery state moves, never at wake; the
  pacer owns the rule and installs it into `/maintain`, `/ticket`, `/align` and `/recall`; the
  position number is the one carrier and `.0040` derives the table from it. A second item beside
  it: whether the pacer still waits behind the four refactors now that R5 and `.0110` give its
  rules a form and a target.
- **`.0110`, planned to closed** ([the ticket](../tickets/done/01-0010.0110-project-facets-injected.md),
  [the RFC](../rfc/done/01-0010.0110-project-facets-injected.md)). `local.rules.md` beside the
  entry file is the injector's second source, read as the reserved slug `local`, placed after
  every mechanism's block and refused where it would not be last, with an `overrides` bullet
  resolved against the cited rule's declared targets and rendered where the reader meets it.
  Nine `<project-local>` blocks left seven core files; the tag is retired and the check reports
  one left behind. The evidence bullet left the mechanism-doc format, since an installed
  paragraph cannot parse as a header bullet: the evidence is a declared record and
  `docs/mechanisms/` a painted door. 243 → 257 tests. Entry contract v13.
- **The entry file's *Project-local* section is installed blocks alone** — the shape's R7,
  saying where a project's answers live, then the project's own two facts. The verification set
  is `/verify`'s local block, beside the definition it answers.
- **A live round trip at `/verify`**, on the user's instruction: all four sources retracted,
  twelve blocks gone with none left outside a fenced example, twelve reinstalled byte-identical.

## What the user corrected, kept as evidence

- **"How did you decide on what is next?" — "But who wrote it?" — "So how did you decide on
  it?"** Three questions to reach the answer that a previous session of me had written the
  candidate line by advancing a chain, and that this session read it back as a fact of the tree.
  Applied by hand means applied by me, one session writing the next session's wake.
- **"Why is the verification set injected into AGENTS.md and not into `/verify`?"** No reason:
  I had converted the block where it sat. The ticket had reserved the placement for the plan
  and the plan had not decided it. Moved.
- **"Why is the project-local not in the verify skill, though?"** I answered a question about
  anchors. The user meant the rule: **"ffs, of course it needs to be injected. Do it using our
  existing script. Otherwise you make it tier 3."** The tier-1 rule about the local file had
  been planned as a hand-authored part line beside the hand-edit rule, on the argument that a
  meta-rule is what the anchor form is for. A rule that is not installed is not delivered, and a
  hand-written paragraph is the copy the check cannot compare. It is R7 now, installed in the
  entry file and in `/verify`, where a drifted block is found and would otherwise be edited in
  place.
- **"Retract all installed blocks, test that none remain, reinstall, check the diff."** The
  diff was the order of two sibling blocks at one anchor in two files, whose committed orders
  came from opposite install sequences. Nothing depends on it and the shelf now says so.

## Open, with owners

- **`.0130`'s `/plan`** is the queue's candidate by the chain, `next-cycle=ask`. Whether it is
  also next by the ordering rule — `.0040` ranks second by it and the ordering shape leans on it
  — is [01-0020](../tickets/01-0020-pacer.md)'s, held there with the pacer's own placement.
- **Sibling blocks at one anchor stand in install order**, so a recipient's update will reorder
  them. Unowned; the installer ticket is the natural home if it matters there.
- **The three `/plan` choices were taken on my recommendation** when the user said to implement
  without choosing otherwise: the evidence bullet dropped, `/maintain`'s links block deleted, the
  override criterion reworded to its decidable form. Recorded on the RFC and the ticket; the
  user overruled a fourth choice the plan had not listed, the tier-1 rule's form.
- **The install spec's testing bullet** still says an override naming nothing is refused; left
  as the accepted spec's wording, as `.0140` left *path convention*.
- **Carried from nineteen, unchanged:** the remover and inline wrappers; a bare ticket id in
  core prose invisible to every check; 01-0020's rules file for the wake rule; contribution
  back; the user's grade on the mechanism shape; frost_map's `tickets.py` unrun; 01-0017's
  nineteen claims.

## Housekeeping

Six commits of mine on `main` today, `5090231` the last; this record uncommitted; six commits
ahead of `origin/main`, unpushed (`push=ask`). Verification set green: 257 tests, three checks
clean; 63 straw dogs at the close, all bound, no condition met. Entry contract v13. The tree
holds one local rules file and twelve installed blocks from four sources.
