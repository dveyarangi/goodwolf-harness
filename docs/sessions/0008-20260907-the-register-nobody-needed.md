# The register nobody needed, and a document with an owner for every sentence

**Date:** 2026-09-07
**Historical handoff:** current state belongs to the [delivery queue](../tickets/README.md) and the
owning tickets. This record is a snapshot of the eighth session, in Claude Code. `/conclude` is not
installed; this was written by hand at the user's request.

## Work completed

- **`/commit` gained `DREAM`**, a third prefix outside the ring beside `FIX` and `EQUIP`. The
  second lobe produces work and no prefix named the step that produced it, so the first dream could
  only have been committed under a prefix claiming a different producer.
- **The first dream committed**, from session seven.
- **`/plan` on [01-0011.0010](../tickets/done/01-0011.0010-mechanism-declared.md)** — its
  [RFC](../rfc/done/01-0011.0010-mechanism-declared.md) written and then validated three times.
- **[01-0010.0120](../tickets/01-0010.0120-host-delivery-surfaces.md) minted**: what each host can
  place in front of an agent without being asked.
- **`docs/process.md` measured section by section**, and `AGENTS.md`'s `Installed:` line deleted.
- **[01-0010.0110](../tickets/done/01-0010.0110-project-facets-injected.md) gained dream language** as a
  bootstrap facet — the first that is an expression choice rather than a command.

## Settled decisions and their owners

- **The register is derived; no file holds it** *(the user)*. Supersedes *the register is
  `.agents/README.md`* on the spec. `.agents/mechanisms/*/` is the enumeration.
- **`docs/process.md` sheds by sentence, in the slice that gives each an owner** *(the user)*. Line
  68 to the glossary, line 72 obsolete, line 70 kept under a `<temporary>` bound to `.0022` so
  `temporary_statements.py` enumerates the next shedding rather than someone remembering it.
  `Current constraint` deleted, contradicted by `AGENTS.md`'s own switches.
- **Both rosters deleted** *(the user)*. `AGENTS.md`'s `Installed:` line and `.agents/README.md`'s
  install narrative were copies of `.agents/skills/`, of the queue, and of each other.
- **`/skill-up` stays a sibling.** Life keeps `skills` as its own mechanism; what transfers is its
  tier-1 meta-rule 10 — *before building or amending a mechanism, use `/mechanism`* — declared as a
  **part**, so retracting the mechanism removes its own trigger.
- **A Part is what an install adds and an uninstall removes.** Life's definition, and it covers
  anchored insertions into files the mechanism does not own, which *a part is a file* could not.

## Open, with owners

- **Nothing runs the check at wake.** Patched into the verification set;
  [01-0010.0120](../tickets/01-0010.0120-host-delivery-surfaces.md) owns whether better exists.
- **`.0030`'s ratchet is *loud when it rises* with no channel to be loud on.** Recorded on that
  ticket, same owner.
- **Named in the loop but not installed** — `/recall`, `/conclude`, `/edge` — is derivable and
  nothing derives it. [01-0010.0100](../tickets/01-0010.0100-remaining-named-corpus.md)'s.
- **The spec described its own provenance wrongly** on two points, corrected: Life allows several
  instruction files, so our one-file rule is this project's tightening.

## Session through the advise questions

**Great:** the user's *"what will read it? isn't our script simply deriving it?"* — one question
that dissolved an accepted decision, an acceptance criterion and a whole `<project-local>` block,
and Life's own declined-writer note supplied the evidence against Life's own practice. **Good
enough:** the four planning passes; every gap in passes two through four sat on one seam — the
machine-read contract between doc and check — which is meta-rule 2 being paid for, not
oscillation. **Questionable:** I posed an A/B fork on where the register lives and recommended B.
Both branches committed a copy. The user's next question dissolved the fork, which means I had
built it on a premise I had not tested. **Missing:** I did not read Life this session until asked,
having leaned on this spec's summary of a read another session did — the exact failure the
previous dream named as its seventh seed, and worse, since I had read nothing at all. **Redundant:**
I planned a `<project-local>` block for `.agents/README.md` before establishing that anything local
needed holding; the user asked what it was for and the answer was nothing. **Out of balance:** I
used `sed -i` once for a queue edit after the standing rule from session seven says edit tools, so
one change went past the user invisibly. Caught and reported in the same turn, but the rule is a
day old and already broken once. **Hidden edges:** a designated place for local facts is not a
container problem but a question problem — twice this session, a block existed with nothing that
belonged in it. **Easier:** a session where `.agents/mechanisms/` exists, so the seam has both ends
written down and a fifth pass has nothing left to find. **Next:** `/implement` stage 1 on
[01-0011.0010](../tickets/done/01-0011.0010-mechanism-declared.md), under `next-cycle=ask`.

## Housekeeping

Verification set green at 72 throughout; no code was written. `/maintain` and `/verify` were edited
by the user mid-session, moving the docstring and TODO rule from the first to the second.
