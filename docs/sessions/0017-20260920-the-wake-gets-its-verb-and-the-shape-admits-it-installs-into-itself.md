# The wake gets its verb, and the shape admits it installs into itself

Session seventeen, 2026-09-15 → 2026-09-20, one conversation opened on an evening and closed on a
morning five days later. It began as a wake that did not wake: "good evening" was answered with a
greeting and an offer of `/recall`, and the user ran it by hand with one argument — how to make
that the default. Everything else followed from answering it. Two commits of mine — `ALIGN` and
`MAINTAIN` — and no push.

## Work completed

Details are on the records named.

- **`/recall`** found the queue as record sixteen left it: the chain `.0140 → .0110 → .0130`, with
  `.0140`'s align the candidate and [01-0011.0050](../tickets/01-0011.0050-shape-checked.md) the
  one AFK slice on the critical path. Nothing in that reading changed by the close; the candidate
  is unchanged.
- **The pacer's first core rule landed** at entry contract **v9**: run `/recall` first in every
  session, whatever the first message says *(the user, who refused a carve-out for a first message
  naming a skill or a task)*. Hand-written beside the announce line and wrapped as a straw dog on
  [01-0020](../tickets/01-0020-pacer.md), because the pacer is undeclared and has no rules file.
  On [pacer.md](../pacer.md#core-rules--under-alignment), the ticket, and
  [the contract record](../research/entry-contract-findings.md#v9--2026-09-20).
- **Rule failure 4 opened, and failure 3 struck once**, on
  [the register](../rule-failures.md). Failure 4 is the wake miss: the instruction lived in the
  loop diagram, a noun-phrase picture, and the only sentence-form rule was scoped to "what's
  next". The strike on 3 is the unwrapped wake rule itself — three causes, none the 2026-09-14
  one: the sentence carried no tell because it is provisional by ownership, not wording; the
  governing ticket said *placed* in the entry file, written one day before the shape said
  *installed, never authored*; and `/mechanism`'s three-homes table admitted "general rules" to the
  entry file while the exception sat three sections down.
- **The cause was fixed, not only registered** *(the user)*. Mechanism-shape gained **R5** — a
  mechanism's rule reaches `AGENTS.md` only as an installed block, and while the mechanism is
  undeclared it is written by hand and wrapped on the inception ticket as it is written —
  installed into `/align` beside P7 and into `/mechanism` itself. The three-homes row now reads
  *meta-rules, and a mechanism's rules only as installed blocks*.
- **Self-install became policy.** Landing R5 into `/mechanism` raised the user's question of
  whether the shape had a notion of installing into its own skill. It did in practice — R1 and R4
  had gone into `/mechanism` since 2026-09-07, and the doc named the failure mode — while every
  rules-file title, the format template and the parts table said *skills this mechanism does not
  own*. The policy is now a sentence in `/mechanism` with its why in
  [the doc](../../.agents/mechanisms/mechanism-shape/mechanism-shape.md): a rule read only in its
  own act is authored in the body; a rule two occasions read is in the rules file and installed
  everywhere it is read, the owner's own skill included. Five titles and the method glossary's
  entry now say what the practice was.
- **`/maintain`** over the touched files: queue header and the pacer's status to 2026-09-20, the
  resume note now says a session's first act is `/recall`, the moments table carries the occasion
  R5 instructs, and the glossary residue of *does not own* was the one thing the retitle had
  missed.

## What the user corrected, kept as evidence

- **"What did you just miss with your agents.md edit?"** I had appended the new rule to the
  announce sentence as a trailing clause — failure 3's shape, reproduced inside the fix for
  failure 4, in the same turn the register was being written. It became its own sentence opening
  with the verb.
- **"Who is the owner of this new rule?"** Nobody. A mechanism's rule, hand-authored into the
  entry file, with no rules file to install from and no straw dog on the ticket that would give
  it one. The wrap and the ownership analysis followed.
- **"Recording as failure is not enough. We need to fix the cause right now."** The register
  entry had ended in *proposed amendment, not landed*. The decisive fact for the fix came from the
  sweep: no rules file has ever targeted the entry file, so *installed, never authored* had never
  once governed `AGENTS.md`, and my sentence was the house style, not the exception.
- **"Is this second target self-install? Do we have a notion of self-install vs direct?"** The
  shape did it and said the opposite. Policy stated, titles corrected.
- **"No need any specifics. Any session wake should start with recall."** I had offered a
  carve-out with a recommendation; the user's rule is simpler than mine and is the one that landed.
- **"It's morning."** I wrote *tonight* across a session that spanned five days; the records carry
  the dates as they are.

## Open, with owners

- **`.0140`'s align** is still the next pass and still blocks the chain. `.0050` beside it,
  Ready since 2026-09-09. Next cycle needs a nod (`next-cycle=ask`).
- **01-0020 now carries a debt**: inception owes the wake rule a rules file and an
  `<installed by>` block in `AGENTS.md`, which makes the pacer the **third claimant** on the
  entry file as an injection target after `.0110` and
  [01-0017.0010](../tickets/01-0017.0010-terms-defined-before-they-land.md). One anchor
  convention, whichever lands first. Still nobody owns the coordination. The pacer's remaining
  decisions — resume state, step boundary, chaining, whether `/pacer` is a skill — stay open on
  the ticket.
- **Two grades pending.** Failure 4 is graded by the next session's first reply: `/recall` before
  anything else, or the entry is struck. Failure 3's strike is graded by the next mechanism rule
  written into the entry file: wrapped on its ticket, or the strike counts twice.
- **The tier-1 count** is on 01-0017.0010 as an observation: every sentence in `AGENTS.md` naming
  a skill is hand-authored, and a guesser tell for it would fire on most of the file. Evidence for
  that ticket's *one fact in two homes* question, not a repair.
- **v9 is taken**; `.0110`'s bump is v10.
- **Stale tickets against later rules** have no general fix: 01-0020's *placed* bullet was
  corrected by hand, and re-checking records when what governs them moves is
  [01-0011.0060](../tickets/01-0011.0060-mechanism-rechecked-when-governing-moves.md)'s shape,
  scoped to mechanisms. Tickets would be a second shape for it; not minted.
- **Carried from sixteen, unchanged:** the entry file's straw-dog sentence on `.0130`'s *What to
  build*, contribution back on the spec, the user's grade on the mechanism shape, and frost_map
  running `tickets.py` unrun.

## Housekeeping

`6420e56` at close, two commits ahead of `origin/main`, unpushed (`push=ask`), tree clean before
this record. Verification set green: 216 tests at ~74s, four checks clean; twenty-three straw dogs,
all bound, no condition met. Entry contract v9.
