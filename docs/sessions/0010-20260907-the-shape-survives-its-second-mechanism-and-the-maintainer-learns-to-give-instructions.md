# The shape survives its second mechanism, and the maintainer learns to give instructions

**Date:** 2026-09-07, running past midnight.
**Historical handoff:** current state belongs to the [delivery queue](../tickets/README.md) and
the owning tickets. This record is a snapshot of the tenth session, in Claude Code, the first
written by `/conclude`. Wake was still by hand: `/recall` is not installed.

## Work completed

One full ring on [01-0011.0022](../tickets/done/01-0011.0022-shape-survives-second-mechanism.md),
`/align` → `/plan` → `/implement` → `/verify` → `/maintain`, closing it and
[01-0011.0010](../tickets/done/01-0011.0010-mechanism-declared.md) together. Eight commits.

- **`/maintain` is declared** through the shape, and **the check admitted it unchanged**:
  `mechanisms.py` is byte-identical to before the slice. The shape was amended three times in
  `/mechanism`'s prose and once in the format shelf — installed text rather than injected
  pointers; a sentence about another mechanism is that mechanism's; *Mechanical by
  construction*; one code span per absence cell — never the declaration bent to fit. That is the
  result the slice was placed to produce, and it is recorded on both closed tickets.
- **The clock is its own ticket**,
  [01-0011.0060](../tickets/01-0011.0060-mechanism-rechecked-when-governing-moves.md), minted after
  Life's `maintenance` mechanism was read directly and falsified `.0022`'s premise: a maintenance
  mechanism is record-bearing by design, so `/maintain`'s record obligation is `not yet`, not
  `none by property`.
- **`/maintain`'s body is instruction, not story**: 29 numbered lines, A1–F3 plus R1–R5 and
  P1–P4, no strength labels, no attribution, no skill named but `/align`. What other mechanisms
  will install is demarcated as one block per owner inside a `<temporary>` bound to the ticket
  that installs it. `docs/process.md` shed four sections; the evidence moved to
  `docs/mechanisms/`; 112 → 115 tests.
- **The queue stopped restating its tickets**: the *Completed step* paragraph is a pointer now.

## Settled decisions and their owners

All on the closed ticket unless named. The ones that reshaped more than this slice:

- **`/maintain` holds four things in agreement** — docs to the meta-rules, docs to their
  implementation both ways, live records to their format, every fact to one home — replacing
  *three subjects*. Landing-time agreement is `/verify`'s; `/maintain`'s occasion is drift. The
  facet on [01-0010.0110](../tickets/01-0010.0110-project-facets-injected.md) is now *which
  documentation-and-implementation pairs the project has*.
- **Meta-rules are never injected**; they live in `/mechanism` or the entry file.
- **A sentence about another mechanism is that mechanism's** — installed where it sits, never
  authored there. Landed in `/mechanism`'s body at once. Life runs it: `maintenance` installs
  into `/mechanism`.
- **Strict versus working rule was withdrawn** the same evening it was adopted: strength serves
  the maintainer at amend time, and the repair policy already decides who may amend what.
- **The seven rules of `mechanism-shape.rules.md` and their two targets** are
  [.0020](../tickets/done/01-0011.0020-rules-one-home.md)'s input; `maintain.rules.md` exists
  already, one rule targeting `/mechanism`'s *Incept*.
- **Anchor repairs by hand, reported** — twenty-three citations in eight records.

## Open, with owners

- **Next cycle needs a nod** (`next-cycle=ask`). The queue names
  [.0020](../tickets/done/01-0011.0020-rules-one-home.md): both declarations carry hand copies
  waiting for the injector.
- **Two graders, two questions**, pre-registered on the closed ticket's doc: the user grades
  whether the shape changed because `/maintain` did not fit it or the declaration was bent;
  `.0025`'s session grades whether the doc received the installed block unrewritten.
- **A skill structure with addressable rules, and an instruction-not-prose check** →
  [01-0010.0100](../tickets/01-0010.0100-remaining-named-corpus.md), `/skill-up`'s owed change.
  `/maintain`'s body is the first instance; the second decides whether it generalises.
- **Three findings on [01-0017](../tickets/01-0017-io-graph-coherent.md)**: a whole-tree link
  check; `/verify:38` and `/implement:38–40` naming rules or skills that are not theirs; and a
  mechanism doc's `not yet` rows linking specific tickets under `docs/`, which *Core and
  instance* may or may not exempt.
- **The mover does not rewrite a backticked path** → [.0025](../tickets/01-0011.0025-archive-duty-reaches-maintain.md),
  found when both docs' `declared by` bullets dangled after the close.
- **The clock's five align questions** on `.0060`, the first being whether Git is the clock.

## Session through the advise questions

**Great:** the user's *"how does it help the executing agent?"* about strict versus working. It
did not, and saying so cost one glossary entry and a column of labels — the second time this
day a distinction died on that question, after *three subjects* died on *"are those
symmetrical?"*.

**Good enough:** the second `/plan` pass, which read the check's parser line by line and found
three rows the doc would have failed and one table it would have half-read, before anything was
written.

**Questionable:** I wrote the body as prose twice — first with rationale and attribution on
every line, then still with explanation — before the user pointed at `/skill-up`'s first rule.
The rule was in front of me the whole session.

**Missing:** the moment *telling an inceptor what to hand `/maintain`* was put on the wrong doc
and caught only by re-reading the glossary's definition of **Moment** at the second pass.

**Redundant:** another session was writing into the same tickets during the align, in words
matching mine, so several edits landed twice and had to be deduplicated. The user said to ignore
it; the deduplication cost a round each time.

**Out of balance:** the ticket carries a four-hundred-line align record for a slice whose
landed body is one hundred and ten lines. E2 permits it; nothing yet says when a ticket's
deliberation is folded.

**Hidden edges:** every one of the second pass's findings was a parser fact — first code span,
one path per row — that no document stated. The format shelf now states them; the check never
had to change.

**Easier:** a `--check` that renders a moments count by kind, so a record's *five instructed,
four not yet* is read rather than counted by hand and corrected at `/verify`.

**Next:** [.0020](../tickets/done/01-0011.0020-rules-one-home.md), on the user's nod.

## Housekeeping

Verification set green at every commit: 115 tests, `mechanisms.py --check` over two
declarations, the enumerator at six statements, all bound, no condition met. Zero dangling
citations at close. Four records moved in one invocation; the queue's rows and both headers
updated by hand, as P3 says.
